# Funded DETF refactor — current execution status

The implementation remains in the main repository on `codex/robinhood-release-review`, preserving unrelated changes. **The complete plan is not yet validated.** The machine-readable queue is `current-implementation-queue.json`; criterion-level evidence is `acceptance-progress.json`. Historical checkpoints, including superseded failures and approval blocks, are preserved in `execution-checkpoint-history-through-20260908.md` and their original JSON/log files. Historical wording does not restore a pending decision or excluded scope.

## Owner decisions and applied cleanup

The owner explicitly approved removal of obsolete V4 mature-close configuration, getters, storage, initialization, readers/writers and dead code. The cleanup was regenerated against current main-repository files and applied, including `closeRouteMode` / `closeRoutes`, obsolete user-LP close accounting and the unreferenced legacy V4 bond target. The actual V4 facet retains common funded bond claims, linear vesting, independently claimable staking rewards and standard exchange/SY exits. The subsequent storage audit also removed the write-only V4 `epochAnchor`; `lastExpansionTimestamp` retains the first-bond epoch clock.

`v4-close-cleanup-owner-approval.json` records the explicit authorization. Ten selected compiled API/schema/source checks and affected frontend checks passed. Fresh full-build closure verification passed for all 16 funded facet/package records, with zero stale import closures and all deployable artifacts within EIP-170. It will be rerun after any subsequent source changes. **No cleanup approval remains pending.**

D60 excludes Balancer-hosted DETFs from functional changes; compilation maintenance only. Unrelated Balancer SE work remains in scope. D66 explicitly defers unfinished Slipstream work and its release validation; existing completed sources/tests/evidence are preserved. Neither creates an unresolved release requirement.

D65 authorizes removal by the current live `feeTo()` Fee Collector in restricted mode, including after rotation. Additions remain DETF-only; former collectors lose special withdrawal access and can transfer their LP. Both policies retain public swaps. Selected four-family/two-policy, external-LP, own-LP bond and collector-rotation checks passed. The separate standing staking-reward NFT rights remain unchanged.

## Active validation

The complete default repository build passed: 5,638 files with Solc 0.8.35 in 20,580.773 seconds. Its original JSON/log are archived as `default-full-build-5638-green.*`; the accompanying scope record clarifies that default discovery does not cover all maintained scripts. Current sources stayed fixed throughout the build.

The corrected focused selection executed 48 cases across 22 suites: **47 passed, one failed**, in 98.421 seconds (Forge suites 25.48 seconds). Both V4 Lido modes, custom quote/custody checks, current Stata deployment protection, the Orbital launch stage and the other migrated reentrancy controls passed. The Orbital hostile fixture failed during setup because all three required token-decimal arguments were unset. Its correction now uses the same canonical helper as the ordinary fixture and retains the security assertions. Evidence: `post-build-47-pass-orbital-fixture-failure-*`.

The earlier empty-fork-URL failure and empty test selection are archived separately and are not counted as validation. Runners now unset direct fork settings, use the corrected regression-name selection and reject a successful process exit with missing test execution.

The post-validation batch is applied: the Orbital fixture correction, five EtherFi source/fork changes, and the exact stale V4 decimal comment cleanup. The maintained-script compiler errors are resolved; `current-implementation-queue.json` records the active session. Solidity sources are frozen until it actually exits. EtherFi remains **unvalidated**; the current queue records the remaining fork and existing hermetic coverage.

The source-less script stack-depth error was isolated using the exact default compiler settings: 289 script roots passed and five failed through `ResearchFixture_OrbitalHook._sampleJson`. Its JSON values now use a memory struct and bounded concatenations; all original string literals remain identical and ordered. The Robinhood testnet exporter also now reads actual deployed token decimals, including nine-decimal DETF/sDETF. Both changes are applied and the combined maintained-script rebuild passed: all 294 roots and 1,376 compiled files in 602.173 seconds (`maintained-script-294-green.json`). The isolation result does not replace the combined build or runtime validation.

Default discovery does not cover all maintained deployment scripts. After preserving the actual default result and resolving focused failures, `run-full-build.py --include-scripts` explicitly builds complete `contracts`, `test/foundry/spec` and `scripts/foundry` roots under unchanged settings. This includes 294 maintained/current script sources. The other 14 research sources concern DualLiquidity, deleted before this refactor by commit `c0631349` and explicitly retired by the PRD. They and the 63 historical `scripts/archive` sources remain preserved and are not active release callers. `maintained-script-build-scope.json` records exact scope. Current package execution remains a separate rehearsal gate.

## Latest completed evidence

- Lido: eight actual SE proxy tests passed (build 66.360 seconds; test command 1.983 seconds). Four actual Lido protocol projection tests passed at Ethereum block 24,000,000 (build 50.169 seconds; test command 9.027 seconds), including fractional share rounding, external conversion, usage fees and whole projected-state equality. The V4 nested-payment regression now passes in both CP liquidity modes.
- Stata: eight actual proxy projection tests passed (build 144.243 seconds; test command 12.784 seconds). The later code-bound deployment salts and occupied-namespace regression also pass in the current focused selection.
- Composed quote checkpoint: 39 pass / four fail across 15 suites after a successful 406-file build (1,556.931 seconds; test command 15.890 seconds). The four Orbital failures omitted additional LP minted by residual sweeps. Assertions now reconcile all actual LP mint logs, preserve exact principal/backing/fee checks and require exact donation minimum/rollback; the subsequent 47-pass checkpoint validates these corrected custody/quote assertions.
- Active frontend: lint, typecheck and all 410 tests across 59 files passed after refreshing the V2 package ABI (58.748 seconds).
- Earlier native SE coverage: 29 proxy suites represent 25 in-scope issuers in the 603-pass checkpoint. Its three size failures were subsequently repaired; final current-source inventory and complete validation remain required.
- V4-backed Orbital: all 343 selected regressions passed with actual 31,000,000-gas call budgets. Ordinary deposits measured 11.45–12.14M; the retained fuzz counterexample measured 26.54M. Read-only live Robinhood transaction-limit evidence is 32,000,000. Finite tests do not establish a universal gas bound.

These focused counts overlap and must not be added or represented as a complete repository pass. Original logs and exact commands remain the evidence.

## Remaining engineering and release gates

1. Finish exact composed-route provider compatibility without an unapproved deployment allowlist. The existing Lido and Stata implementations are validated as above. EtherFi target/facet/package/factory changes and five actual-protocol fork cases are in `etherfi-composed-quotes.patch`, **applied, compilation and runtime validation pending**. Six read-only actual protocol input calls succeeded at block 24,000,000; this proves input availability only. `run-etherfi-live-projection-fork.py` is prepared, not executed. Other provider compatibility is tracked in `custom-route-provider-compatibility-review.json`.
2. Validate the applied post-validation batch and fix any actual failures only after active validation exits. No Slipstream edits are included.
3. Complete the full default-plus-maintained-script build and full hermetic suite; attribute exact source/contract/test failures against the recorded non-green baseline and resolve in-scope failures. Preserve D60/D66 attribution.
4. Deploy current packages and run the approved lifecycle rehearsal on the strict local Robinhood fork at `localhost:18663`, chain 4663, pinned block 56,118,361, EIP-170 limit 24,576 bytes and 32,000,000 block gas limit. The node and core pins are ready; current package/lifecycle execution has not run. Artifacts remain visible under `current-robinhood-rehearsal/`. No public deployment is in scope.
5. Finish current-source selector/storage/package manifests and `reconcile-funded-selector-manifest.py --require-current`, compiled test inventory, acceptance mapping and measured consolidation report. The final gate checks compiler metadata hashes for the full import closure, not only an existing ABI.

## Baseline and consolidation

The immutable baseline is `e08b415733e729471779dcb8102529631330ea16`. Its full build passed in 37,045.157 seconds; the hermetic retry produced 30,056 pass / 3,632 fail in 9,718.795 seconds across 3,068 suites, with five documented misplaced network-source exclusions. Do not rerun or overwrite this evidence.

Current source reconciliation covers 3,111 files: 6,497 versus 5,236 directly declared test/invariant names. In-scope/shared declarations are 4,934 versus 4,330. All 59 inventoried Slipstream declarations remain. Supplementary records map 393 retired declarations across 55 sources to retained assertions or approved requirement changes. These are source traceability counts, not inherited executable-case counts or runtime proof. Baseline compiled inventory records 3,073 concrete test contracts and 36,563 ABI test/invariant methods before the five network exclusions; the completed current compiled inventory records 2,699 concrete contracts and 31,239 methods. In-scope/shared methods are 29,085 before and 27,529 now; their compiled artifact size increased. See `compiled-test-consolidation-comparison.json` for separate D60/D66 accounting.

The identical 14-case fixture comparison passes before and after; two expensive setups use about 95% less EVM gas, while total command wall time increased. No full-suite speedup is claimed. The remaining estimate is the explicitly uncertain 3–5 working days recorded in plan §12.1; actual build/test costs are preserved separately and the estimate must be refreshed after full validation.

## Current validation sequence — 2026-09-08

EtherFi passes all five actual-protocol projection cases at Ethereum block 24,000,000, after a source-root artifact-discovery correction. All five production components fit EIP-170. Camelot passes all 19 real-proxy cases. Evidence: `etherfi-live-projection-fork-run.json` and `camelot-projection-referral-receipts-run.json`.

Session 52340 runs the complete contracts/default tests/294-maintained-scripts build, then the targeted follow-ups and complete current hermetic suite with baseline attribution. Solidity is frozen until that entire sequence exits. `aerodrome-projection-prepared.json` records an unapplied exact LP-fee projection and six regressions for the existing Aerodrome fixture. No new deployment argument or provider allowlist is introduced by that draft. D60/D66 remain unchanged. Full plan acceptance remains open.

### Rocket Pool composed-provider draft during source freeze

The read-only Ethereum block 24,000,000 preflight resolves actual deposit-pool version 3, a 0.05% deposit fee, a 0.01 ETH minimum, finite queue-based capacity and almost no immediately available rETH collateral. The local canonical source is version 4; the prepared quote model accounts separately for its direct rETH collateral refill. Validator assignments reduce pool balance and queue capacity equally, allowing exact projection of their difference.

`rocket-projection-prepared.json` records ten proposed source changes, not applied while release validation session 52340 remains active. The draft appends and validates RocketStorage in instance deployment arguments, using the registry option already documented by the Rocket Pool PRD. In-memory production compilation passes (input facet 17,055 bytes, package 10,244 bytes); seven proposed live-protocol checks typecheck. These are diagnostics, not execution or release acceptance. `run-etherfi-live-projection-fork.py --provider rocket` reuses the maintained staking fork runner once sources can be applied and built.

Rocket draft follow-up: public reads also confirmed actual deposit-pool version 4 at Ethereum block 25,934,585 (`rocket-projection-input-preflight-25934585.json`). The same seven-case fixture and runner now support both fixed blocks (14 required executions), with separate v3/v4 evidence files. Current in-memory production and typecheck inputs match the prepared patch; the final input facet is 17,290 bytes and package 10,244 bytes. No canonical Solidity was changed and no Rocket runtime case has executed yet.

### Prepared provider follow-ups during the active full build

The ERC-4626 receipt correction is prepared without changing canonical Solidity. It preserves receipt-denominated standard routes and passthrough custody, and prefers the verified ERC-4626 accounting path when a nested vault also exposes differently priced SE routes. Draft production compilation passes (input facet 22,266 bytes; package 9,414 bytes); six new methods typecheck, including two actual nested production proxies. The consolidated provider runner will execute all twelve old/new transition cases. The Aerodrome draft now has seven typechecked projection cases, including pool-held LP and unsynced token donations; its combined native/projection selection requires at least 34 cases. These are pending execution, not acceptance evidence.

The active full compiler and its focused checks remain uninterrupted. The complete hermetic run on that intermediate source hash is explicitly deferred until the prepared provider corrections are applied; the final full suite and exact baseline attribution remain required (`hermetic-pre-provider-deferral.json`).

The ERC-4626 draft additionally covers proportional vaults without virtual offsets, as required by the existing sfrxETH integration. Its latest diagnostic input facet is 23,540 bytes (package unchanged at 9,414 bytes), and all six new hermetic plus four new fork methods typecheck. Public input reads at Ethereum block 24,000,000 confirm the actual no-offset floor/ceiling conversions and that the existing ETH minter is enabled (`sfrxeth-projection-input-preflight.json`). No actual projection execution has yet occurred.

LP accounting follow-up: prepared V2/Camelot/Aerodrome quotes for the retained LP accounting asset. Consolidated with the Aerodrome patch to keep one edit to the existing fixture. Draft production sizes: Aerodrome query 23,895; Camelot query 16,940; V2 query 15,669; Aerodrome package 14,046 bytes. Seven Aerodrome and three shared methods typecheck. Actual proxy execution remains pending; session 52340 still owns the canonical source freeze.

Position storage follow-up: nine-source unapplied cleanup removes V3/V4 unread pool-state and liquidity caches plus V4 never-created wings. All 26 affected deployment components compile within EIP-170; eight existing test sources typecheck. No runtime acceptance claimed, no public API removed, no excluded/deferred product edited. See position-storage-cleanup-prepared.json and position-cleanup-draft-{production,typecheck}.json.

Prepared ten existing V3/V4 fixture migrations for mandatory two-token activation. Native ETH multi routes must follow PoolKey order after mapping currency0 to WETH; numeric ERC20 ordering would incorrectly reject a legitimate native pool. The shared validation correction is included in the unapplied position cleanup. Existing decimal/base sleeve, native ETH, fee/import and reentrancy tests are preserved and migrated. Expanded draft typecheck passes; canonical Solidity remains frozen for the active full build, and actual test execution remains required.

Expanded V3/V4 follow-up drafts now cover forty existing fixture sources and nine production sources. All 26 production components compile within EIP-170 (36.091s); 212 selected source files typecheck (1,650 reported cases, 13.704s), and 34 representative source files generate test bytecode (288 reported cases, 181.439s). The combined provider/position application check matches all 69 changed sources. `position-fixture-case-migration-map.json` maps every changed test name: 291 explicit declarations become 287, with zero unmapped removals; decimal leaves and their coverage remain. This is prepared-only evidence. Actual current-source Forge execution and final release acceptance remain required.

Prepared provider/position checkpoint (2026-09-08T22:56:06.389041+00:00): 74 source changes, including 45 existing fixture sources, remain unapplied while full validation session 52340 runs. Exact input coverage checks pass for every prepared change. V3/V4 diagnostics: {"typecheck": {"source_files": 248, "methods": 1920, "seconds": 79.626}, "test-codegen": {"source_files": 44, "methods": 356, "seconds": 398.958}}; 26 production components fit EIP-170 (largest 23163 bytes). The retained runtime inventory is 248 suites / 1,920 methods; none of this draft evidence counts as execution. Fixture declarations map 349 to 345 with no unmapped retirement and all decimal leaves preserved. Actual provider, position, full hermetic and strict local rehearsal validation remains required.

Active handoff (2026-09-09T00:00:34.190593+00:00): `continue-checked-followups.py` is running as PID 77898 / PTY 52715. It waits for the whole previous parent PID 49915 / PTY 52340, requires a successful matching full build and passing actual post-build regressions, archives those records, applies the 74 exact checked source changes once, retires the intermediate zero-execution hermetic deferral, and runs `run-provider-position-followups.py --label funded-followups` serially. Do not apply the patch separately or start another Forge while this handoff is active. Any prerequisite/build/test failure stops the handoff for review. Its progress record is `checked-followup-handoff.json`. The final complete build/hermetic/baseline attribution, strict local Robinhood package/lifecycle rehearsal, and acceptance reconciliation remain required after focused success.

Applied follow-up checkpoint (2026-09-09T00:18:49.933627+00:00): the combined default contracts/tests plus 294 maintained scripts build passed in 23,391.542 seconds. All 48 actual post-build tests passed across 22 suites; the earlier Orbital fixture failure is resolved. The whole previous sequence exited, and its exact records/logs are archived in `pre-provider-build-checkpoint/`. The guarded handoff then applied all 74 checked main-repository source changes and preserved before-file backups. The 45 fixture hashes match the applied case map (349 to 345 declared methods, no unmapped retirement). The intermediate hermetic deferral is retired; complete final hermetic execution remains required. Focused validation is running under PTY 52715; the V2 build passed with all four components within EIP-170. No second Forge or canonical Solidity mutation while this sequence runs.

The provider/position batch stopped after the complete position run (1,943 pass / 54 fail, 265 suites / 1,997 cases). Nine existing fixtures are corrected and their affected concrete suites are running under PTY 10263. Every declaration count is retained; the V4 decimal import now uses the actual vendored PositionManager. No production Solidity changed in this follow-up. Rocket and sfrxETH forks and all final release gates remain pending.

All 333 affected methods / 46 suites pass (zero failures/skips), resolving all 54 actual position failures. The remaining V3 import metadata cleanup and multi-interface ordering comments are applied under source fingerprint 96d7a3ff1d5d6d8f41f1ff0c7a8dd09419e8243b669dd27700325822edf63dde. PTY 4255 now runs the pinned Rocket v3/v4 and sfrxETH forks, followed by a matching complete build, unfiltered hermetic suite and baseline attribution. Preserve the source freeze and single-Forge execution. Strict local deployment/lifecycle and final acceptance remain required.

All 18 remaining actual-provider fork cases pass (Rocket v3 7, Rocket v4 7, sfrxETH 4), with production component size checks passed. PTY 4255 has entered the complete default build including 294 maintained scripts; the unfiltered hermetic run and attribution follow automatically. Final source snapshot remains 96d7a3ff1d5d6d8f41f1ff0c7a8dd09419e8243b669dd27700325822edf63dde.
