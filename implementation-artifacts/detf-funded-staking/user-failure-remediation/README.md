# Reported failure remediation

Seventeen existing Solidity sources were changed after the owner reported 28,775 passing and 1,362 failing tests. The patch is applied; post-fix runtime validation remains pending. The reported footer is separate evidence and has not been assigned to an archived run or source fingerprint.

| Failure group | Change |
| --- | --- |
| Balancer mixed-leg and multi-pair buffer swap panics | Convert scaled18 callback input into native transfer units; keep virtual accounting scaled18. Existing decimal tests now assert exact payment, exact virtual growth and zero physical buffer balance. |
| Common-buffer virtual balance assertions | Compare virtual balances to WAD increments, while retaining native token inputs. |
| Orbital donation transfer failures | Carry projected SE state through both zap swaps, including withdrawal overpayment and rebalancing; calculate LP shares and required deposits from the resulting state and amounts. Remove the stale pre-swap approximations. |
| UniV3 inverse quote gas exhaustion | Use forward-verified interpolation before binary search, retaining the minimum sufficient shares and existing withdrawal buffer in live and projected quotes. Runtime gas remains unverified. |
| Production-provider launch and token-gate fixtures | Select opening prices from measured reserve valuation; assert each route's own synthetic price and creation rate. |
| Weighted last-claim/donation fixtures | Use 10-token later purchases within the weighted AMM input domain while retaining full claims, donation and subsequent-holder isolation assertions. |
| Funded staking and decimal fixtures | Obtain real DETF through a mature funded claim and unstake, then stake through sDETF's own exchange surface. Permit at most 10 native units of CP/UniV3 residual dust and check that it cannot mint hook LP. |

D60 excludes further functional changes to Balancer-hosted DETFs. Their reported failures remain visible. D66 unfinished Slipstream work remains deferred. AMM math limits, compiler settings, fuzz settings and test gas limits were not changed.

## Evidence

- Before edits: a warm diagnostic reproduced seven failures and one passing control across six suites, with compilation skipped. See [diagnostic log](../user-failure-diagnostics.log).
- Compiler preflight: 12 production contracts/libraries and nine representative test suites, containing 237 test methods, generated bytecode without errors. The production runtimes are all below 24,576 bytes; the largest is 23,343 bytes. A final isolated V3 check verified the exact-minimum boundary correction.
- No post-fix Forge build or runtime test was started. Compiler checks left `out/` and `cache_forge/` untouched and do not establish corrected runtime behavior, gas usage or release acceptance.
- [Applied source hashes and compiler evidence](applied.json), [reviewable patch](remediation.patch), [first compiler check](first-codegen.json), [final V3 compiler check](v3-minimum-codegen.json).

Current source/configuration fingerprint: `26484c06fffc5dc4aaf6fcaf8054f8dac5448e3b394f1181207cee58f08ca911`.

## Next owner-run validation

From the repository root:

```sh
python3 implementation-artifacts/detf-funded-staking/run-current-repository-checks.py --label user-failure-remediation
```

The maintained runner preserves earlier evidence, builds the complete contracts/tests and maintained script roots, runs the unfiltered default hermetic suite, and attributes failures. A completed failing test run still proceeds to attribution. Review every remaining in-scope failure before accepting the change. The retained fork checks, strict local Robinhood rehearsal and final acceptance reconciliation remain separate requirements.
