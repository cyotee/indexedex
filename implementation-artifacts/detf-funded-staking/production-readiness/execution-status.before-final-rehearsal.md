# Funded DETF — current production readiness

The owner reports that all tests pass. That baseline is recorded with explicit unknown run/build provenance in [owner-passing-baseline.json](production-readiness/owner-passing-baseline.json). Historical failures and interrupted processes are preserved; they are not current failure claims.

The authorized [remaining implementation plan](../../contracts/vaults/detf/DETF_PRODUCTION_READINESS_REMAINING_IMPLEMENTATION_PLAN.md) is executing. Final-output guards and an unmintable self-leg correction resolve two V4 rounding findings; all 11 direct boundary, constructive fuzz, raw-SY and composed-unstake regressions passed after a production build.

The subsequent deployment-core review confirmed missing authorization on three Crane canonical-registry writes (PR-04). Guards and all eight production-factory authorization regressions now pass against the corrected complete build. The pre-fix full build completed with exit code zero in 15,790.852 seconds. Its source-change guard correctly stopped before stale tests; the corrected complete build passed in 27,719.527 seconds and all 27 regressions passed with zero failures/skips. The unfiltered suite passed all 31,305 tests with zero failures/skips. Affected provider forks still require renewal. The strict local rehearsal must bootstrap a fresh corrected core rather than reuse the vulnerable pinned public registry.

Frontend checks passed: 59 files / 410 tests, lint and typecheck. The pinned Base and Robinhood RPC blocks are available. Eight retained V3 fork cases and the strict local package deployment/39-case lifecycle matrix remain required. The older rehearsal node is historical; the two fresh strict nodes and their preserved evidence are identified below.

Current process/evidence pointers are in `current-implementation-queue.json`. Acceptance remains open until the final artifacts, security review, manifests and required runtime checks match. No public network deployment or migration is authorized. D60 Balancer-hosted DETFs and D66 unfinished Slipstream remain excluded/deferred.

Earlier execution status is archived at [before-execution-status.md](production-readiness/before-execution-status.md). See [P0_BASELINE.md](production-readiness/P0_BASELINE.md) for provenance limits.

An earlier source checkpoint added the required strict-rehearsal direct staking and raw/staking SY round trips, retaining all 39 cases and 30M transaction limits. Candidate source SHA: `255327c8003ecaf332aba822dbb4a1accb932e339ea2fd8405470b903bb63658`. That checkpoint and its session 35991 coordinator were superseded before they launched Forge; the current candidate and coordinator are recorded below.

### 2026-09-10: funding-quote completeness correction

PR-06 adds missing catalog stage 06/09 to the architecture simulation, honors the isolated broadcast directory, and rejects empty/incomplete gas inputs. Eight shell checks and exact 19-stage parity pass; full simulation remains pending. The earlier waiting coordinator was stopped before it launched Forge, archived, and restarted as session 55594 for source `2dab3ca0e3307eef624b38fee597f569513a1b6c699207fcbd642b8fb19c1ff5`. The original compiler was not interrupted. Both Ethereum provider pins are available.

The [workspace preservation check](production-readiness/workspace-preservation-check.json) reconciles all 5,365 task-start files: none missing and only ten intended files changed. The two Crane patches have separate before-copies and hash records. The [draft release manifest](production-readiness/release-manifest-draft.json) records the current source and evidence hashes with `HOLD_REQUIRED_EVIDENCE_OPEN`; it awards no acceptance and must be superseded by a fresh final manifest after the required runs and manual acceptance reconciliation.

The isolated strict node is now running as session 14343 at `http://127.0.0.1:18663`, pinned to block 56,118,361, Prague, chain 4663, with a 32M block gas limit and an explicit 24,576-byte runtime limit. [Node preflight](current-robinhood-rehearsal-production-readiness/strict-node-preflight.json) passed without deployment transactions. Preserve this node and label; recheck liveness before the P3/P4-dependent package deployment. The original build session 99663 has finished. The active corrected build/test sequence is session 55594; the obsolete session 54472 exited on its expected source guard without starting Forge.

The post-build runner is queued as session 22226 (`production-readiness/post-build-release-core-final/run.json`). It waits for the exact P3 pass, then serially reconciles artifacts, renews provider/V3 forks, deploys only to the fixed local node, verifies code/receipts, runs all 39 lifecycle cases and obtains the complete isolated funding quote. It stops on the first failure and does not close acceptance. The separate funding node is session 21702 at port 18664, still at the untouched fork pin. Its `strict-node-preflight-v2.json` passes; the first preflight incorrectly compared the historical upstream header limit with the local node setting and is preserved as diagnostic history. New local blocks and transactions still require 32M gas limits. Both local runners now seed isolated caches so they cannot prune the complete default build cache.
