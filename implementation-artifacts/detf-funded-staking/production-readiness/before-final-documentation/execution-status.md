# Funded DETF — current production readiness

The owner-reported all-tests-pass baseline is preserved with unknown original command/counts/build fingerprint in [owner-passing-baseline.json](production-readiness/owner-passing-baseline.json). It is not represented as an agent-executed run.

The corrected candidate is local Solidity/configuration SHA `231e505d8379c41deb1905d933d32e2b9974f358f84bce169d056f07d4384c75`, Crane SHA `b45a057611f782040ee29f1fddbbcc8eeba01def2c9341ef01c6bb65f685e123`. Its complete build including 294 maintained scripts passed in 27,719.527 seconds. All 31,305 unfiltered default tests in 2,695 suites pass with zero failures/skips, as do all 27 overlapping security/boundary regressions. All 27 provider forks and eight retained V3 cases pass at their exact pins. Frontend lint/typecheck and 410 tests pass; 28 supported ABI declarations and identical renderer evidence reconcile.

The active post-build coordinator is session **80191**, [resume4/run.json](production-readiness/post-build-release-core-final-resume4/run.json). It reuses the ten matching completed machine checks and is deploying the current architecture only to `http://127.0.0.1:18663`. The fresh corrected core is required: the pinned public core lacks canonical-registry authorization and is prohibited for this release. The first attempt failed before broadcast on local fee metadata; its logs/manifests remain archived. PR-07 aligns staged local simulation/broadcast fee arguments; six command checks pass, and the retry has successfully deployed the corrected core/manager and is continuing package stages.

Required next: actual core/package/receipt reconciliation; all 39 strict lifecycle cases including direct stake and raw/staking SY; complete isolated 19-stage funding simulation; final acceptance and release manifest. The coordinator stops on a failed check and does not mark acceptance complete. The node remains chain 4663, Prague, pinned at upstream block 56,118,361, with 24,576-byte runtimes and 32M new blocks. Lifecycle money-path calls retain 30M bounds.

Session 14343 owns the completed-work node at port 18663. Session 21702 owns the separate untouched funding fork at port 18664. Preserve both; no reset or public broadcast is authorized. The P3 sequence (session 55594) and earlier post-build sessions 22226, 68674 and 10765 have finished; their successes and reviewed stops remain historical records. `current-implementation-queue.json` retains the P3 sequence as `active_sequence_record` for source-matching prerequisite checks and identifies the actual current coordinator separately.

Final source/selector/storage/native-SE/caller/test-consolidation records are current. The unavailable immutable-baseline Git object is handled by verified archived before-side descriptions, with current descriptions recomputed and zero unmapped in-scope retirements. No speedup is inferred from incompatible full-run timings. All 5,365 task-start files are preserved; intended edits have before copies. Acceptance remains open until the local gates pass. D60 Balancer-hosted DETFs and D66 unfinished Slipstream remain excluded/deferred. No public deployment, customer activation or fund migration has occurred. This internal review is not an independent external audit.

Earlier checkpoint prose is retained in [execution-status.before-final-rehearsal.md](production-readiness/execution-status.before-final-rehearsal.md) and [before-execution-status.md](production-readiness/before-execution-status.md).

The final launch-only correction PR-08 populates the required Balancer stable hook exit/query facets through its existing FactoryService. Only that library and its two script entrypoints are affected; all production contracts, tests, dependencies and compiler configuration are unchanged. The explicit complete build including all 294 maintained scripts passed in 132.233 seconds after compiling those three sources. The 31,305 hermetic, 27 targeted and 35 pinned-fork passes retain their original executed provenance and are reused through `production-readiness/pr08-script-only-evidence-reuse.json`; they are not represented as rerun results. Local deployment resumes only stage 06/09 and export, then the 39-case lifecycle and complete funding simulation.
