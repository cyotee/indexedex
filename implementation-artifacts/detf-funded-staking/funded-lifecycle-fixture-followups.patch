--- a/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/UniswapV4Detf_Alignment_CloseD25Base.sol
+++ b/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/UniswapV4Detf_Alignment_CloseD25Base.sol
@@ -177,7 +177,7 @@
     function _liveAliceBob() internal returns (uint256 aliceId_, uint256 bobId_) {
         _ensureActors();
         (aliceId_,) = _bondAs(d25Alice, 40 ether);
-        (bobId_,) = _bondAs(d25Bob, 20 ether);
+        (bobId_,) = _bondAs(d25Bob, 4 ether);
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
     }
 
--- a/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/UniswapV4Detf_ClaimOpenBase.sol
+++ b/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/UniswapV4Detf_ClaimOpenBase.sol
@@ -28,7 +28,7 @@
 
     function test_postMaturity_claimPaysFundedStaking() public {
         _firstBond(100 ether);
-        (uint256 id_,) = _firstBond(40 ether);
+        (uint256 id_,) = _firstBond(20 ether);
         _assertFundedMatureClaim(detf, id_, detfUser);
     }
 
--- a/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/UniswapV4Detf_ReserveDonationOpenBase.sol
+++ b/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/UniswapV4Detf_ReserveDonationOpenBase.sol
@@ -398,7 +398,7 @@
     function test_DN6_twoBonders_navRisesTogether() public {
         _ensureLiveBond();
         address bob_ = makeAddr("donation second bonder");
-        (uint256 bobId_,) = _bondAs(bob_, 80 ether);
+        (uint256 bobId_,) = _bondAs(bob_, 10 ether);
         bytes32 bobBefore_ = keccak256(abi.encode(IDetfBondNFT(address(_nft())).positionOf(bobId_)));
         DnLiveSnap memory before_ = _snapLive(dnUserOriginal);
         uint256 lp_ = _donatePair(dnDonor, 15 ether);
@@ -536,7 +536,7 @@
     function test_DN14_closeAfterDonate_userBasketUnchanged() public virtual {
         _ensureLiveBond();
         address bob_ = makeAddr("donation funded claim");
-        (uint256 id_,) = _bondAs(bob_, 60 ether);
+        (uint256 id_,) = _bondAs(bob_, 10 ether);
         IDetfBondNFT nft_ = IDetfBondNFT(address(_nft()));
         uint256 principal_ = nft_.positionOf(id_).principal;
         _donatePair(dnDonor, 9 ether);
@@ -552,15 +552,15 @@
         _ensureLiveBond();
         address bob_ = makeAddr("donation last prior holder");
         address carol_ = makeAddr("donation next holder");
-        (uint256 bobId_,) = _bondAs(bob_, 40 ether);
+        (uint256 bobId_,) = _bondAs(bob_, 10 ether);
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
         _claimFundedDonationPosition(detfInfo, dnUserBondId, detfUser);
         _claimFundedDonationPosition(detfInfo, bobId_, bob_);
         _donatePair(dnDonor, 20 ether);
         uint256 lp_ = _lpToken().balanceOf(address(_nft())) + _lpToken().balanceOf(detf);
         assertGt(lp_, 0, "all prior claims preserve donated protocol liquidity");
-        (, uint256 principal_,,) = detfInfo.previewBond(_openPairToken(), 30 ether, DEFAULT_MIN_LOCK);
-        (uint256 nextId_,) = _bondAs(carol_, 30 ether);
+        (, uint256 principal_,,) = detfInfo.previewBond(_openPairToken(), 10 ether, DEFAULT_MIN_LOCK);
+        (uint256 nextId_,) = _bondAs(carol_, 10 ether);
         assertEq(IDetfBondNFT(address(_nft())).positionOf(nextId_).principal, principal_, "next bond only receives its quoted funded purchase");
         assertGe(_lpToken().balanceOf(address(_nft())) + _lpToken().balanceOf(detf), lp_, "new bond cannot capture old LP");
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
@@ -620,8 +620,8 @@
         uint256 raw_ = _buyDonationRaw(10 ether);
         address bob_ = makeAddr("disabled bond owner");
         address carol_ = makeAddr("disabled staking holder");
-        (uint256 bobId_,) = _bondAs(bob_, 50 ether);
-        (uint256 carolId_,) = _bondAs(carol_, 40 ether);
+        (uint256 bobId_,) = _bondAs(bob_, 10 ether);
+        (uint256 carolId_,) = _bondAs(carol_, 10 ether);
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
         _claimFundedDonationPosition(detfInfo, carolId_, carol_);
         vm.prank(owner);
--- a/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/decimals/UniswapV4Detf_Alignment_CloseD25Base_Decimals.sol
+++ b/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/decimals/UniswapV4Detf_Alignment_CloseD25Base_Decimals.sol
@@ -60,7 +60,7 @@
     function _liveAliceBob() internal returns (uint256 aliceId_, uint256 bobId_) {
         _ensureActors();
         (aliceId_,) = _bondAs(d25Alice, _uPair(40));
-        (bobId_,) = _bondAs(d25Bob, _uPair(20));
+        (bobId_,) = _bondAs(d25Bob, _uPair(4));
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
     }
 
--- a/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/decimals/UniswapV4Detf_ClaimOpenBase_Decimals.sol
+++ b/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/decimals/UniswapV4Detf_ClaimOpenBase_Decimals.sol
@@ -28,7 +28,7 @@
 
     function test_postMaturity_claimPaysFundedStaking() public {
         _firstBond(_uPair(100));
-        (uint256 id_,) = _firstBond(_uPair(40));
+        (uint256 id_,) = _firstBond(_uPair(20));
         _assertFundedMatureClaim(detf, id_, detfUser);
     }
 
--- a/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/decimals/UniswapV4Detf_ReserveDonationOpenBase_Decimals.sol
+++ b/test/foundry/spec/vaults/detf/protocols/dexes/uniswap/v4/detf/decimals/UniswapV4Detf_ReserveDonationOpenBase_Decimals.sol
@@ -320,7 +320,7 @@
     function test_DN6_twoBonders_navRisesTogether() public {
         _ensureLiveBond();
         address bob_ = makeAddr("donation second bonder");
-        (uint256 bobId_,) = _bondAs(bob_, _uPair(80));
+        (uint256 bobId_,) = _bondAs(bob_, _uPair(10));
         bytes32 bobBefore_ = keccak256(abi.encode(IDetfBondNFT(address(_nft())).positionOf(bobId_)));
         DnLiveSnap memory before_ = _snapLive(dnUserOriginal);
         uint256 lp_ = _donatePair(dnDonor, _uPair(15));
@@ -458,7 +458,7 @@
     function test_DN14_closeAfterDonate_userBasketUnchanged() public virtual {
         _ensureLiveBond();
         address bob_ = makeAddr("donation funded claim");
-        (uint256 id_,) = _bondAs(bob_, _uPair(60));
+        (uint256 id_,) = _bondAs(bob_, _uPair(10));
         IDetfBondNFT nft_ = IDetfBondNFT(address(_nft()));
         uint256 principal_ = nft_.positionOf(id_).principal;
         _donatePair(dnDonor, _uPair(9));
@@ -474,15 +474,15 @@
         _ensureLiveBond();
         address bob_ = makeAddr("donation last prior holder");
         address carol_ = makeAddr("donation next holder");
-        (uint256 bobId_,) = _bondAs(bob_, _uPair(40));
+        (uint256 bobId_,) = _bondAs(bob_, _uPair(10));
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
         _claimFundedDonationPosition(detfInfo, dnUserBondId, detfUser);
         _claimFundedDonationPosition(detfInfo, bobId_, bob_);
         _donatePair(dnDonor, _uPair(20));
         uint256 lp_ = _lpToken().balanceOf(address(_nft())) + _lpToken().balanceOf(detf);
         assertGt(lp_, 0, "all prior claims preserve donated protocol liquidity");
-        (, uint256 principal_,,) = detfInfo.previewBond(_openPairToken(), _uPair(30), DEFAULT_MIN_LOCK);
-        (uint256 nextId_,) = _bondAs(carol_, _uPair(30));
+        (, uint256 principal_,,) = detfInfo.previewBond(_openPairToken(), _uPair(10), DEFAULT_MIN_LOCK);
+        (uint256 nextId_,) = _bondAs(carol_, _uPair(10));
         assertEq(IDetfBondNFT(address(_nft())).positionOf(nextId_).principal, principal_, "next bond only receives its quoted funded purchase");
         assertGe(_lpToken().balanceOf(address(_nft())) + _lpToken().balanceOf(detf), lp_, "new bond cannot capture old LP");
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
@@ -542,8 +542,8 @@
         uint256 raw_ = _buyDonationRaw(_uPair(10));
         address bob_ = makeAddr("disabled bond owner");
         address carol_ = makeAddr("disabled staking holder");
-        (uint256 bobId_,) = _bondAs(bob_, _uPair(50));
-        (uint256 carolId_,) = _bondAs(carol_, _uPair(40));
+        (uint256 bobId_,) = _bondAs(bob_, _uPair(10));
+        (uint256 carolId_,) = _bondAs(carol_, _uPair(10));
         vm.warp(block.timestamp + DEFAULT_MIN_LOCK + 1);
         _claimFundedDonationPosition(detfInfo, carolId_, carol_);
         vm.prank(owner);
