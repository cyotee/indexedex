## 12. Completion record

The complete current default build passed on 2026-09-08: **5,638 files, 20,580.773 seconds**, unchanged compiler settings. Original evidence is `default-full-build-5638-green.*`; its scope clarification distinguishes default roots from maintained deployment scripts. The fresh funded selector/source-closure gate passed all 16 records with no stale closures and all deployable artifacts within EIP-170. Focused follow-ups are running after correcting a runner-only empty fork URL; the first attempt executed no tests and is preserved as `post-build-empty-fork-url-failure.*`. Full hermetic, maintained-script compilation and current local deployment acceptance remain required.

The maintained deployment callers live under `scripts/foundry/`, while the current Foundry file has no `script` override. The default build therefore does not establish compilation of every caller. After its actual completion, preserve that result and run `run-full-build.py --include-scripts`, which explicitly includes `contracts`, `test/foundry/spec` and `scripts/foundry` without narrowing the default test roots or changing compiler settings. Current-source hermetic validation follows the successful combined build. This covers 294 maintained/current script sources. The other 14 sources under `scripts/foundry/research/dualLiquidityLinkedCrossVersion` belong to the product deleted in commit `c0631349`; §1 of the alignment PRD explicitly says that product is removed and will not be repaired. The first explicit script build exposed its already-deleted test-base import (`combined-script-retired-dual-liquidity-failure.*`). Preserve those historical script files without resurrecting the removed product. They and the 63 sources under `scripts/archive` are not active release callers. `maintained-script-build-scope.json` inventories every included and historical source; all contracts and default tests remain in the build. Script-stage execution remains a separate local rehearsal.

Final selector/size reconciliation runs `reconcile-funded-selector-manifest.py --require-current`. It checks the compiler metadata Keccak hashes for each artifact and its imported sources, distinguishes the shared abstract metadata base from deployed facets, and requires every deployed facet/package to fit EIP-170. These checks supplement actual proxy wiring and current local deployment; an ABI from a stale artifact cannot close acceptance.

Current criterion-by-criterion status and evidence: [acceptance progress](../../../implementation-artifacts/detf-funded-staking/acceptance-progress.json). This record distinguishes selected validation, known provider failures, excluded/deferred work, and the applied owner-approved A30 cleanup. The full plan remains incomplete.

Execution remains in the main repository on `codex/robinhood-release-review`, preserving unrelated changes and D60/D66 scope. The explicit mature-close cleanup is applied and all ten compiled API/source checks pass. A further storage audit removed the write-only V4 `epochAnchor`; the actual first-bond clock remains in `lastExpansionTimestamp`. Current funded storage has 30 V4 fields, with each retained field mapped to its live purpose and references.

The latest integrated build passed (406 files, 1,556.931 seconds); its selection passed **39 of 43 tests across 15 suites**. All composed custom-input bond/donation quotes passed their principal/output comparisons; the four remaining failures were Orbital custody assertions that omitted additional LP from the retained residual sweep. The tests now reconcile all acquired LP against actual mint events and also enforce exact donation minimums and atomic rollback. Validation of these follow-ups remains pending. Direct prepaid receipt fixes, V2/V3/V4/ERC4626 external-deposit projections, epoch-entry ordering, current query cuts and the real Orbital dependent-rate SY exit passed. All current narrow query facet/library size checks pass.

The earlier **603-pass/67-suite** selection covered all 29 native-SE proxy suites for 25 in-scope issuers. Its deployment-size failures were repaired. Frontend lint/typecheck and **410 tests across 59 files** passed the approved cleanup; the new V2 package ABI has since been refreshed from the compiled package and its fresh frontend check also passed all 410 tests, lint and typecheck in 58.748 seconds. These selected counts overlap and do not establish complete repository acceptance.

Current engineering work includes preserving composed-route compatibility with SEs that lack the optional provider projection extension. Exact Stata-backed projections are implemented in the existing target/facet/package and compile within EIP-170; eight real proxy sequence/deposit tests passed. The subsequent code-bound factory/package salt change and occupied-namespace regression are included in the current full build. Lido projections now pass eight actual SE proxy tests and four real-protocol Ethereum fork tests at block 24,000,000, including fractional rounding and whole-state equality. V4 nested conversions use exact acquired-amount approvals and clear them afterward, with new Lido fallback/bond tests in both CP liquidity modes awaiting execution. No deployment allowlist or new owner decision is introduced. The strict local Robinhood fork is ready for current-package deployment and lifecycle rehearsal. See `current-implementation-queue.json`, `execution-status.md` and `acceptance-progress.json` for evidence and remaining work. No approval remains pending for the cleanup, Fee Collector redemption, Balancer exclusion or Slipstream deferral.

The full default build exposed older excluded-family type and fixture-initialization references. They are being repaired as D60 compilation maintenance; no excluded family economic behavior or Slipstream functionality is being refactored. The main Robinhood workflow now includes the current registry-deployed Orbital hook package, architecture export and staged rehearsal pin. Its replay/registry/facet-size test is in the existing launch-stage suite and awaits runtime validation. Exact records: `full-build-type-compatibility-and-orbital-stage-fixes.json`, `excluded-inherited-selector-compatibility.json` and `excluded-composed-harness-initialization-compatibility.json`.

The independent baseline remains `e08b415733e729471779dcb8102529631330ea16`: its complete hermetic result was 30,056 pass / 3,632 fail with five documented network-source exclusions. A current full default build, complete hermetic run and attribution against that non-green baseline are still required. Focused suite counts overlap and must not be added. The current source reconciliation covers 3,111 test sources against the immutable baseline, with 6,497 directly declared test/invariant names before and 5,236 now. The in-scope/shared subset is 4,934 before and 4,330 now across 1,989 files; historical excluded-Balancer changes are separated, and the 24 inventoried Slipstream sources retain all 59 declarations. These are lexical declaration counts, not inherited executable-case counts or proof of a speedup. All in-scope files with retired declarations link to a consolidation record; the supplemental mapping resolves 393 declarations across 55 sources to retained tests or approved requirement changes. The two duplicate network sources remain in the canonical fork tree. See `current-test-consolidation.json` and `retired-test-anchor-consolidation.json`. Runtime validation remains required. The identical 14-case fixture comparison passes before and after; two expensive setup cases use about 95% less EVM gas, while complete command wall time increased. This is not evidence of a full-suite speedup. See `policy-fixture-performance-comparison.json`. No full stage is marked complete yet.


