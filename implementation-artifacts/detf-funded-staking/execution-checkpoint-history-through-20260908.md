## Latest owner approval — V4 mature-close cleanup

The owner explicitly approved deletion of the obsolete V4 mature-close deployment configuration, getters, unused storage, initialization, readers/writers and associated dead code. Update callers, tests, ABIs and docs; preserve funded claims, linear vesting, staking rewards and standard exchange/SY exits. `v4-close-cleanup-owner-approval.json` records the exact approval. The prior automatic-review rejection is historical and no longer leaves a pending owner decision. The cleanup has been regenerated and applied across 22 current files; validation is in progress. D60 Balancer DETF exclusion and D66 Slipstream deferral are unchanged.

# Current release scope — D66

Defer all unfinished Slipstream work from this release by owner decision. Robinhood targets Uniswap V4. Preserve completed Slipstream changes, existing functionality, tests and validation evidence. Permit only compilation maintenance or compatibility edits required by shared V4 changes; no pending Slipstream functional, full-range/import conversion, accounting, native SY, deployment/UI, test, fork or integration completion gate remains. Shared V4 work and other approved scope continue; no unresolved release requirement is created.

The acceptance matrix is now A1–A42, with A33 excluded by D60 and A34–A36 scoped to V3/V4. See `slipstream-deferred-release.json`. Remaining estimate: **3–5 working days**, including **12–24 hours build/test wall time**; details and uncertainty are in plan §12.1. This replaces earlier wider-scope estimates.

## Latest validation checkpoint

An extended Orbital gas-range regression supersedes the earlier all-green focused status: `orbital-gas-range-first-*` records **341 passed / one failed across 44 suites** after a successful build. The additional actual V4-backed retained-LP bond passes. The new native-SY amount fuzz found an out-of-gas input (`false, 16222`) under the enforced 31,000,000 call budget. The input is retained as a fixed regression, and the original fuzz range and run count are unchanged. A second optimization caches the unchanged SE holder balance and prior claim once during input inversion. Its separate build and focused tests pass: **81 passed / zero failed in ten suites** (orbital-cached-claim-81-green-*). The fixed counterexample now deposits using 26,538,730 gas; the ordinary route matrix uses 11.45–12.14M. Actual proxy calls retain the 31,000,000 budget. The broader selection also passes: **343 tests / zero failures across 44 suites** (v4-cached-orbital-343-green-*). These are still focused checks, not completion of the full plan.

`v4-cached-withdrawal-and-collector-340-green-*`: **340 passed / zero failed across 44 suites**, after a separate successful build (37.678 seconds; test command 79.889 seconds). This includes the four V4 reserve families and both policies, own-LP funded bonds, actual external LP ownership, funded standard swap fallback, live Fee Collector rotation/internal SY, native SE/SY routes, real V4-backed Orbital and release packaging. The latest production gas optimization also passes independent exact-input quote comparisons, budget/refund tests and all installed facet/execution-delegate size limits. This was overlapping focused evidence, not a full-repository pass.

The later cached-claim optimization brings ordinary V4-backed Orbital deposits to 11.45–12.14M gas and the retained gas counterexample to 26.54M; redemptions remain 4.24–6.40M in the warmed test. The default build has progressed past the encountered excluded-Balancer compile errors and now stops on V4 legacy test APIs (main-full-build-v4-retired-mint-call-failure.*). V4 legacy API/test consolidation remains unfinished. The separate recorded cleanup review block is unchanged. Slipstream remains owner-deferred with no release requirement or question.

## Current D65 resolution

The owner approved restricted LP redemption through the current `feeTo()` Fee Collector, including after rotation. Implementation now adds dynamic removal authorization and an owner-operated collector withdrawal; additions remain DETF-only. `pending-restricted-fee-lp.json` is resolved. Selected four-family/two-policy checks now pass, including actual protocol-fee LP, collector rotation, internal SY caller context and owner-operated redemption (`v4-native-sy-and-collector-300-green-*`, `v4-funded-fallback-and-packaging-108-green-*`). Full-release validation remains incomplete. The original standing staking-reward NFT beneficiary rule is unchanged.

# Current scope override — latest owner decision

D60 excludes Balancer-hosted DETFs from functional refactoring. Stop that work; further edits there are compilation maintenance only. This leaves unrelated SE vault work in scope, including Balancer SEs and accepted V3/V4 changes. D66 subsequently defers unfinished Slipstream changes. Composed opening changes and tests already started remain historical work; do not expand or finalize that excluded implementation. The combined validation already running when this scope change arrived was allowed to finish; its evidence remains historical.

D61–D64 accept immutable `ownerOnlyLiquidity` in creation/DETF/hook configuration, own-V4-reserve LP bond payment valued from non-DETF assets only, whole-LP custody, settled pre-transfer valuation, `G = 0`, and existing funded bond economics. External LP remains outside protocol-owned accounting in both permission modes. The Balancer LP-payment question is retired in `pending-lp-bond-route.json`.

**D65 resolved:** the owner approved redemption by the current `feeTo()` Fee Collector, with dynamic lookup on rotation. Additions remain DETF-only and ordinary public removals remain restricted. See `pending-restricted-fee-lp.json` for the investigation and accepted result. `requirement-decisions.json` is the current question/decision index. Historical paragraphs below do not restore the excluded scope or retired question.

The independent baseline retry completed: 30,056 passes / 3,632 failures, 3,068 suites, 9,718.795 seconds; five documented misplaced network sources excluded. The baseline is not green. The Composed frontend changes made before scope exclusion passed lint/typecheck and 408 tests in 59 files (`composed-opening-frontend-check.log`). All nine Aave tests pass (`aave-green-dual-artifact-failure-*`); the later `v4-dual-sequential-sy-green-*` and 300-test checkpoints also validate Dual after its artifact and sequential-quote corrections.

# Funded DETF refactor execution status

Work is in progress in the main repository directory, `/Users/cyotee/Development/projects-defi/daosys/lib/indexedex`, on its existing `codex/robinhood-release-review` branch. The owner authorized relocation on 2026-09-07 after confirming no other agents were using it. All 961 refactor paths were transferred and verified against the isolated implementation delta; no overlapping edits required reconciliation. `relocation/applied.json` records the 601 copied and 360 removed paths; `relocation/main-before-transfer.tar.gz` preserves their prior main-directory contents. Baseline snapshot `e08b415733e729471779dcb8102529631330ea16` still identifies preexisting work, which is not attributed to this refactor. All further implementation work uses the main directory. The previous implementation run has finished; its results and warm Foundry artifacts have been copied into the main directory. The separate pre-refactor baseline remains independent.

## Historical 468-test checkpoint

`checkpoint-468-run.json` and its build/test logs record **468 passing tests in 58 suites** after a successful separate build. Build elapsed 329.937 seconds (6 files); test command elapsed 292.685 seconds, with Forge reporting 282.57 seconds. These timings were collected while the independent baseline test run was active; they do not establish a speedup or regression. This is focused evidence, not full-plan or release completion. Composed opening-price and position-vault design requirements are now resolved by D56–D59; implementation remains pending. At that historical checkpoint, reserve-LP bond-payment disposition was unanswered; D60 now retires the Balancer question and D62–D65 resolve the V4 route and restricted fee-collector redemption.

The checkpoint includes common funded staking/bond math, all eight DETF bindings, both DETF SYs, and native SY for ERC4626, Uniswap V2, Camelot V2, Aerodrome, Lido, EtherFi, Rocket Pool, Aave Stata, Morpho Blue, Single constant-product V4 buffer, Weighted, Curve Quad and Balancer Stable buffer hooks. Balancer Stable adds six native SY tests plus all 88 existing two-through-five-token lifecycle tests and 21 staged-initialization tests. Its former oversized liquidity implementation is split into join/exit/query facets. All deployed runtimes fit EIP-170: join 24,474 bytes, exit 23,117, query/SY 21,505; runtime data is in `balancer-stable-buffer-runtime.json`. The existing fee/yield, mixed-decimal, share-input and single-token exact-output behavior remains green in that matrix.

The TrustFlags, Surface, ExactOutput and Accounting suites migrated obsolete LP-backed expectations to funded production fixtures or their unique pure-math regression. `staking-test-consolidation.json` records 31 assertion dispositions. Sequence tests cover paid bonds, stake/transfer/unstake, later funding, unsolicited backing separation and pooled bond fraction isolation.

At that historical checkpoint, Single CP SY had only raw/pair inputs and its SE-share quote was pending. The later 300-test checkpoint validates that route. V3/V4 native position checks also subsequently passed; the full integration inventory remains incomplete.

## Historical requirement decisions (current scope above)

**Resolved — Composed Stable (D56):** carry opening price and seed ratios into deployment arguments and support a rich launch like other DETFs. Do not hardcode 1:1 first-bond pricing. Replace the provisional implementation and update its deployment/caller/test paths before marking A16/A33 satisfied.

**Historical resolution — V3/V4/Slipstream (D57–D59; unfinished Slipstream later deferred by D66):** deploy across the maximum usable tick range; preserve ordinary V3/V4 behavior, align Slipstream and convert imported positions. Use V2-style proportional ownership and constant-product accounting, exact CL deployed-token math, sleeves and earned fees counted once. Require both tokens initially; retain subsequent single-token deposits and sleeve operation during pool locks. Implement and verify A34–A36; no choice of a one-token NAV or adapter deferral remains pending.

The current question record is `requirement-decisions.json`. These owner decisions resolve requirements, not implementation/test completion.

The now-retired historical route conflict concerned Multi-Weighted and Mixed-Buffer reserve-LP bond payments (`pending-lp-bond-route.json`). Their old LP-only purchase branch minted no DETF and recorded an LP principal claim. The valuation question raised for those Balancer DETFs was retired by D60 when their functional refactor was excluded. It is not an unanswered release requirement, and no further functional migration of those routes is authorized. D62–D65 separately resolve the in-scope V4 route.

## Historical verification before D60/D66

Four additional primary mint cases pass in `balancer-primary-mint-run.json`: real payment joins, exact preview/output, minimum-output rollback, immediate funded rewards and protocol LP growth across the Balancer families. All four external-LP primary burn cases now pass in `balancer-primary-burn-run.json` (build 229.439 seconds; test command 13.75 seconds, Forge 4.74 seconds). They check the exact protocol-owned-LP / complete raw-DETF-supply formula, actual LP burn logs, preview/payout parity and untouched outsider LP. The outsider fixture uses the ordinary Balancer Router and Permit2. Its earlier authorization failure is retained in `balancer-primary-burn-route-failure-*`.

After checkpoint 374, eight selected real-family tests passed in `balancer-final-and-standing-run.json`: mature combined claim and standing-recipient full unstake followed by new bond funding in all four Balancer families. The shared reference independently computes weight top-ups, both allocation floors, the ordinary rebase ceiling, fee/creator receipts and dust. It checks that oracle `feeTo` changes do not move the existing role NFTs. A further selected run, `balancer-final-and-two-bond-epochs-run.json`, passed all eight final-unstake / two-bond epoch cases. Final payouts preserve reserve LP and raw supply, retire the NFT, and redeem delivered sDETF for exact held DETF. Both real bonds receive proportional funded growth, can claim rewards while vesting and cannot claim the same reward twice. They are now included in the complete checkpoint-388 focused run.

Five obsolete Balancer test matrices plus a superseded temporary probe (201 source/base/wrapper files) have assertion-by-assertion dispositions: `balancer-close-test-consolidation.json`, `retired-open-expansion-tests.json`, `balancer-standing-test-consolidation.json`, `balancer-redemption-test-consolidation.json` and `balancer-expansion-test-consolidation.json`. They replace LP liabilities, Open-only expansion, separate role reward claims, LP-unwind redemption and capped expansion with the funded requirements. Distinct reserve/payment-decimal route tests remain for migration. The capped, unused `DETFNaturalExpansionLib` implementation and three cap-expectation helpers were removed; twelve canonical epoch math tests replace the old pure corpus plus its temporary funded companion (`expansion-math-consolidation.json`). The complete focused build/test passes after that cleanup and the added default-configuration / epoch-on-unstake checks (checkpoint 388).

Retained reserve donations pass all four real-family tests. A shared Balancer unbalanced-join quote replaces the old proportional approximation, includes native units/rates/yield fees, and follows Mixed Buffer's actual buffer-to-SE-share route. Single, multi, mixed and composed callbacks support donated existing DETF without printing staking receipts. These gift routes preserve the retained no-expansion-settlement rule. Exact quote/minimum rollback, payment and LP deltas, caller authorization, unchanged raw supply, staking state, epoch boundary and bond position are checked. Query placement within the Single and Multi facet sets preserves EIP-170 limits.

V4 first-bond discovery excludes the separately minted DETF liquidity leg. Its new payment preview shares the opening-rate sizing helper with execution. Weighted, Orbital and Curve Quad tests compare every quoted payment with the wallet's actual debit and reject the preview once the reserve is live.

The frontend passes **405 tests in 59 files**, lint (existing unrelated warnings) typechecking in `frontend-v4-bootstrap-check.json`, and a production build in `frontend-v4-bootstrap-build.json` (47.871 seconds). The first-bond UI detects multi-vault, mixed-buffer and composed selectors and preserves each contract's payment order and native token decimals. It verifies funded-NFT semantics before approvals, rejects stale route/asset changes, and dry-runs the purchase before requesting a transaction. Live bond purchases also require the new funded claim view, so a reused old purchase selector cannot silently select an LP-backed bond. Composed UI/deployment payloads must now carry the accepted opening-price and seed-ratio configuration (D56). V4 full-book first bonds now display every required payment and approve each token from a fresh preview.

Native Balancer BPT SY/SE routes pass 49 assertions across all seven real pool packages. Each uses its existing shared pool-info facet wiring, with no new deployment dependency. The routes execute actual Vault joins/exits, use the native BPT ledger and invariant rate, authenticate the unlock callback, reject public pretransfer claims and isolate exact-output refunds/internal SY balances. The facet runtime is 20,643 bytes (`balancer-native-pool-runtime.json`). Native single-token LP routes advertise physical unpaired assets and SE shares. Virtual buffer assets are rejected by discovery, previews and execution: their pool invariant ignores raw balance deltas, and the native balance solver does not quote physical buffer withdrawals. Existing pool swap/reconciliation routes remain intact. The original 12 route failures and their traces are retained as `balancer-native-pool-route-failures-*`.

Positive expansion passes through all four Balancer DETF hosts using actually funded reserve donations. The shared assertions compare independent integer mint calculations before the first boundary, at hour 25 and after seven days. They prove one aggregate raw mint, funded sDETF backing, unchanged ordinary gons and protocol LP, removal of the former supply brake, fixed completed boundaries and repeated-settlement no-ops. Composed's existing post-bootstrap test does not establish the newly required configurable rich first-bond behavior (D56).

The NFT deployment and surface suites now test actual funded diamonds. Three old arithmetic/LP-conversion suites were retired; `bond-test-consolidation.json` maps all 48 earlier assertions to named replacements and decisions. Twelve replacement integration tests cover role initialization, funding authority, independent purchased principal, linear owner/operator claims, transfer without resetting vesting, immediate reward-only claims, role/escrow isolation, custody protection, registry authority, final selector wiring and guarded transfers. A fee-role transfer at a completed boundary pays the old recipient before later minting pays the new owner.

The old, unreferenced `RebasingClaimTokenTarget`/`Repo` pair has been removed. `retired-claim-implementation.json` records hashes and reader checks; `claim-storage-disposition.json` records the old-to-funded field disposition and current references. Remaining legacy NFT and specialized Composed code still needs reconciliation. Nineteen predicted-DETF decimal encodings in existing V4 test bases now explicitly use nine decimals before CREATE3 deployment (`predicted-detf-decimal-fixtures.json`). The first three default full implementation builds failed in legacy Balancer test dependencies: LP-close tests, separate fee/creator NFT-claim tests, then a temporary Composed expansion probe importing the retired expansion matrix. Those groups and the probe have since been mapped to funded behavior and retired; further full-build migration remains. The latest full failure is recorded in `implementation-full-build-third.json` (28.959 seconds); the earlier attempts remain as `implementation-full-build-first.json` and `implementation-full-build-second.json`. A full default build has not yet passed.

Both DETF research articles use the shared renderer; all eight sections of each are present in generated HTML (`research-rendered-sections.json`). Two unused custom renderers were removed with hashes in `research-renderer-consolidation.json`. Product copy, rules, narrative, canonical skill and mirrors describe the funded design. Forty earlier DETF documents now carry scoped supersession notes, with their previous hashes in `historical-doc-supersession.json`. No marketing draft was published.

All eight actual Solidity SVG fixtures now have browser-rendered PNGs and a visually reviewed gallery in `bond-artwork/rendered/`. `visual-check.json` binds the review to SVG/JSON hashes; the gallery uses the generated fixtures, not the earlier hand-written drafts. Labels and values remain legible across new, halfway, claimed, mature and standing-role states.

No newly deployed funded stack has been used for live UI money-path verification. The isolated baseline full build completed successfully; its diagnostic hermetic test run is still running and has not been killed or restarted for slowness.

The old protocol-compound corpus adds 40 mapped retirements in `balancer-compound-test-consolidation.json`; the Mixed legacy claim corpus adds ten in `mixed-claim-test-consolidation.json`. Combined with earlier cohorts, 251 Balancer source/base/wrapper/probe files have recorded dispositions. Added compound-selector absence checks and six retained Mixed bootstrap/route/guard suites pass in checkpoint 417. A subsequent selected run passed 20 of 21 Mixed deployment/nesting/price cases (`mixed-nested-price-expectation-run.json`); its sole failure was the obsolete expectation that a closed mint gate reverts. The updated price suite passes both tests in `mixed-default-price-regimes-run.json`, checking supply-neutral fallback and subsequent actual primary burning. Selected test dependencies now live in shared/family TestBases; missing-artifact failures and an intermediate inheritance error are retained. An additional run is validating full funded claims with and without available protocol LP, both direct and host Standard Exchange staking routes, retired selectors and mandatory staking-package deployment. The Mixed Bonding root is also being migrated, while its LP-only purchase regression remains pending and is not part of that focused import graph.

The fourth default build failed on removed Mixed Bonding helpers (67.518 seconds), and the fifth on Mixed Bootstrap lifecycle helpers (36.877 seconds). Those callers have now been updated; full-build migration remains incomplete. Both failures retain source/config/tool provenance in `implementation-full-build-fourth.json` and `implementation-full-build-fifth.json`.

The product-law cohort adds 40 mapped retirements in `balancer-product-law-test-consolidation.json`, validated against checkpoint 439. Together the Balancer cohorts now retire 291 obsolete source/base/wrapper/probe files. Every family's final funded claim passes both with protocol LP held and with all available protocol LP transferred out. Direct and host Standard Exchange stake/unstake round trips succeed 1:1; discovery, preview and actual funded/approved raw-DETF and sDETF bond inputs are rejected. The real Single-SE package deployment rejects a missing staking package.

The sixth full build failed in the retained Mixed donation root (`implementation-full-build-sixth.json`, 27.885 seconds). Its twenty assertions have now been migrated to protocol LP/funded staking semantics. The first focused run passed 12 and failed 8 (`mixed-donation-first-*`); six failures were fixture prank ordering and canonical Permit2 approval constraints. Two exposed an existing Aerodrome raw-asset deposit preview using pre-compound reserves despite execution compounding first. The route preview correction passed the donation and native SY regressions below. Full build/test completion remains pending.

The independent default baseline full build completed successfully after **37045.157 seconds** (6012 compilation units reported at start). It used the existing seeded cache and ran concurrently with focused implementation work; this is not a like-for-like performance comparison. Baseline artifacts remain in the independent baseline checkout; no shared checkout caches were overwritten.

All twenty retained Mixed donation assertions now pass in `mixed-funded-donation-run.json` (build 384.685 seconds across 37 files; test 6.692 seconds). The two real quote failures were caused by the Aerodrome raw-token-to-share preview using pre-compound pool reserves. Its corrected preview uses the actual execution order for both pool and vault state. A subsequent nine-test selected run passed actual accrued-fee native SY deposits for both Aerodrome inputs, all four immutable threshold deployment matrices and all four final funded claims with unsupported host destinations rejected (`funded-threshold-and-aerodrome-run.json`). The complete 468-test checkpoint includes these checks and four added, actually funded inert-route checks.

The unfiltered baseline full test run **crashed** after 111.975 seconds in Foundry's macOS `system-configuration`/HTTP-client setup during fork initialization (`baseline-test.json/log` in the baseline checkout); its earlier completed suites also contain preexisting failures. Exactly five network fork sources were found under the default hermetic source tree. The implementation moved three unchanged sources to their network fork directories and retained canonical identical copies of two duplicates; `misplaced-fork-test-inventory.json` records source/destination hashes. The baseline remains unchanged and a diagnostic default hermetic run explicitly excludes those five files. This is not a passing unfiltered baseline result. Fork validation remains a separate gate.

The mandatory-threshold cohort adds forty mapped retirements (`balancer-threshold-test-consolidation.json`), taking the Balancer total to **331 source/base/wrapper/probe files** with named replacement coverage. The seventh default build failed in the remaining legacy Mixed adversarial fixture (`implementation-full-build-seventh.json`, 110.753 seconds). Its fake SE issuer has been replaced with a registered real Aerodrome SE over a hostile underlying ERC20; the canonical recording token can scope callbacks to the DETF's transfer recipient. All 27 P0 concerns have been migrated to funded claims, canonical unstaking, actual protocol LP custody and unconditional swap-fallback exercise. The root reentrancy, rate-provider, pricing, trust-flag and initial-donation suites pass with the 27 P0 cases: 43 tests across six suites (`mixed-funded-security-run.json`). `mixed-funded-security-migration.json` and `mixed-funded-p0-migration.json` preserve original hashes and test dispositions; all named replacement cases in these migrations are now green.

Read-only review found further legacy Aerodrome exact-output inconsistencies: its zap-deposit inverse assumes a post-deposit denominator while minting uses pre-deposit assets, and several exact-output paths still quote or mint old virtual fee shares instead of compounding actual LP. Regressions and corrections remain to be completed before claiming the complete Standard Exchange surface is consistent.


The eighth default full build failed on the retired Mixed surface helper (`implementation-full-build-eighth.json`, 49.059 seconds). Its surface suite now passes five tests in `mixed-funded-surface-run.json`: independently enumerated standard/bond/info selectors, package-cut/loupe agreement, deployed facet size, actual funded standard staking and subsequent bond purchase, retired selector absence and retained CREATE3 metadata behavior. This adds to the 43 passing Mixed security cases; a combined full focused checkpoint after those changes has not yet run.

The first six new Aerodrome accrued-fee exact-output regressions did not reach their test bodies: their selected deployment fixture was missing a FactoryService artifact (`aerodrome-exact-output-missing-artifact-*`). Production imports for the manager/registry, common vault facets and Aerodrome facets now reside in the TestBases that deploy them, replacing dependence on unrelated funded tests. `factory-artifact-base-consolidation.json` records that change. The selected 75-file compile remains in progress; no exact-output regression outcome or production correction is claimed yet. Prepared production/test scripts remain outside the Solidity source graph until that process exits.

The first diagnostic baseline hermetic test session became unavailable after an interrupted user turn and left no completion result. Its original log remains intact; no success or failure is inferred from partial output. A separate unchanged-baseline retry is running with the same five documented network-source exclusions, recording its invocation and eventual outcome in `baseline-hermetic-retry-*`. The earlier temporary authentication failure when writing the exact-output regression helper cleared after the user resumed the task; it is separate from the still-pending V4 cleanup approval.


The Aerodrome artifact consolidation built successfully in 1557.643 seconds (75 files; solc 1550.31 seconds), then all six accrued-fee exact-output regression bodies failed as intended (`aerodrome-exact-output-before-fix-*`): five overquoted required input and the LP-deposit route underquoted it. The correction now uses actual post-compound LP entitlement and Aerodrome's actual fee-outside-pool zap arithmetic for both quote and execution, removes the old share-supply ceiling on zap exact output, validates prepaid LP before compounding creates any new unbooked LP, and aligns withdrawals with actual held LP when no fee compound updates the older book. Validation is running with nine additional LP passthrough/funding/donation controls, the existing Aerodrome exact-output/security roots, six Mixed reentrancy cases including actual primary issuance, and all four funded Balancer claim suites. No passing result is claimed for this correction yet.


The Aerodrome correction and funded claim/security batch passed **113 tests across eight suites** (`aerodrome-funded-exact-output-and-claims-*`): build 481.385 seconds, test 25.228 seconds. All six previously failing exact-output quotes now pass, along with LP passthrough, prepayment and donation controls, the retained Aerodrome security/exact-output roots, six Mixed reentrancy controls, and all four funded Balancer claim suites. Deployed Aero facet sizes are 21835 bytes (In), 19199 (Out), and 13627 (OutQuery), recorded in `aerodrome-funded-exact-output-runtime.json`.

Ten superseded Multi Weighted claim test files were mapped to the verified funded metadata, halfway/full claims, supported 1:1 redemption and funded unsupported-destination guards (`multi-claim-test-consolidation.json`). This takes the recorded Balancer retirement total to 341 files. The Mixed decimal fixture now inherits the shared funded deployment/lifecycle fixture, with real customizable Aerodrome token books; a single lifecycle test covers eight books instead of duplicating its compiled body eight times. That new matrix and six additional raw-token prepayment controls are still being validated. Legacy decimal test callers and default-build migration remain incomplete.


The last selected run before relocation completed with **50 passing tests and one failure** (`mixed-eight-decimal-books-and-aerodrome-prepayment-*`): build 501.62 seconds, test 22.261 seconds. It includes the single lifecycle covering all eight native books, 17 Mixed funded checks and six Mixed reentrancy checks. The failure exposed a missing prepayment validation before fee compounding in the Aerodrome exact-input asset-A execution path. The execution guard has now been corrected in the main directory; validation remains pending. The already passing exact-output guards remain unchanged.

The first main-directory compile failed after 31.752 seconds on explicit override requirements in the shared decimal adapters (`mixed-decimal-adapter-inheritance-failure-*`). Fourteen adapters now dispatch the three fixture hooks explicitly. A new separate build/test invocation is in progress against the main directory; its selected tests cover Aerodrome, the eight-book lifecycle and concrete decimal deployment/nested-staking suites. No passing main-directory result is claimed yet. Remaining decimal security/donation consolidation is prepared in `migrate-mixed-decimal-security.py`, with a read-only preflight mapping every old assertion; it has not been applied while Forge holds the source snapshot.


The relocated frontend passed its main-directory check in **68.527 seconds** (`frontend-main-check.json/log`): lint with existing warnings, typechecking, and **405 tests across 59 files**. This verifies the moved frontend against its actual main-directory dependencies; it does not establish live UI transaction integration.

## Latest selected validation

`mixed-consolidation-and-new-adapter-regressions-*` records a successful 239-file build (453.182 seconds) and **208 passes / 10 failures across 27 suites** (174.012 seconds). The original 195-test selection, including all 824 Mixed native-book assertions, and all eleven weighted deployment/range tests pass. The ten new regression failures expose missing Aave SY/withdrawal routes and tiny-output rounding, and Dual exits re-buffering payout before settlement. Production corrections are applied in `aave-cross-version-sy-sources.json` and `dual-single-output-correction.json`; their validation is pending. Shared native infrastructure reduced measured matrix gas 73.1%; the selected runtime comparison is not a completed full-suite benchmark.

## Historical implementation checkpoints

The first successful main-directory focused build completed in **1427.882 seconds** (1577 compilation units), followed by **53 passing tests and three failures** in seven suites (`main-decimal-adapter-and-prepayment-*`). All 27 Aerodrome native SY and prepayment cases passed, including the corrected raw exact-input guard. Its deployed In/Out/OutQuery runtimes are 21,861 / 19,240 / 13,627 bytes (`aerodrome-main-prepayment-runtime.json`). The eight-book funded lifecycle also passed. Two decimal NestedPush setups overflowed because six-decimal payments were incorrectly expressed as 18-decimal raw amounts; the remaining root failure armed `expectRevert` before a metadata lookup instead of the exchange call. Both fixture issues have been corrected and are being validated.

The Mixed native decimal/security migration now shares the canonical funded behavior and real protocol deployments. Sixteen native matrices replace 128 repeated concrete wrappers while retaining **824 assertion invocations across eight decimal books**, recorded in `mixed-native-matrix-consolidation.json`. `mixed-decimal-security-consolidation.json` and `mixed-native-amount-migration.json` record the associated fixture and native-payment changes. The first matrix build succeeded in 455.556 seconds (57 files), then the selected run finished with 109 passes and 86 failures in 333.445 seconds (`mixed-native-matrix-first-*`). Failures exposed incorrect fixture scaling of unrated 18-decimal SE shares, metadata calls consuming revert expectations, and an Aerodrome excess-fee zap sized from stale pre-proportional reserves. The fixture now distinguishes unrated SE funding from native buffer/rated-leg funding and caches token precision during setup. The excess-fee zap now reads the actual post-proportional reserves. Exact changes and hashes are in `mixed-native-fixture-and-aero-compound-correction.json`; a new build/test is running. No assertion tolerance was widened, and no passing result or measured performance improvement is claimed for this correction yet.

The corrected fixture run built in **458.336 seconds** (68 files) and finished with **194 passing tests and one failure across 23 suites** in 547.551 seconds (`mixed-native-matrix-funded-*`). All sixteen native matrices passed, preserving all 824 per-book assertion invocations. The sole remaining root Aerodrome P0 failure is exactly one raw SE unit: the actual router transfers one fewer raw token than its already-proportional input when it applies its own floor. The preview now repeats that actual router rounding (`aerodrome-compound-router-rounding.json`). Native matrices now deploy common protocol/manager/packages once per test while still snapshotting and deploying all eight distinct token/SE/DETF books (`mixed-native-infrastructure-consolidation.json`). Validation of both changes is in progress; no speedup is claimed yet. The same next build includes retained Multi Weighted deployment/range assertions plus new real Aave cross-version and Dual-buffer route regressions. Aave and Dual production corrections have not yet been applied, so failures in their newly added routes are expected and will be recorded separately.

- Balancer-hosted DETFs: compilation maintenance only under D60; no further functional coverage expansion.
- Complete native SY coverage for registered SE share issuers remaining in scope after D66, including V3/V4 position-backed and buffer-hook packages; preserve existing external reward treatment. Unfinished Slipstream integration and validation are deferred.
- Expand real deployment, route, epoch-ordering, decimal, adversarial and invariant coverage against all acceptance criteria. Preserve unique historical assertions while removing superseded models and duplicate test matrices.
- Reconcile final selectors, storage readers/writers, deployment arguments, scripts and the active application. The current large source/test inventories are drafts, not completed traceability.
- Complete full build/test, runtime-size and integration checks, compile/runtime measurements, and reviewable commits/PR delivery. No production deployment or migration is authorized.

## Historical approval review — resolved by explicit owner approval

Automatic approval review previously rejected a specific patch deleting obsolete V4 mature-close configuration, getters and storage. The owner has now explicitly approved that exact cleanup, including associated caller/test/ABI changes. Regenerate it against current main files and complete validation; no approval question remains. `v4-close-cleanup-owner-approval.json` records the resolution.


### Historical V4 policy / LP payment checkpoint (superseded by the dated validation records below)

- Applied immutable `ownerOnlyLiquidity` argument, actual-hook validation, hook/DETF getters, frontend setting and encoded payload propagation.
- Applied own-reserve LP bond quote/receipt: snapshot after due expansion, non-DETF proportional family entitlements, one duration multiplier, G=0, whole LP custody, existing funded staking/NFT path. Added `previewBond` and live accepted-token discovery.
- Applied D65 live-oracle removal permission for DETF/current collector, separate owner-only addition permission, and owner-operated Fee Collector proportional redemption. No collector privilege cached at deployment.
- Added a shared production-path regression matrix for four V4 bindings × two policies. Validation is still running. Added actual fee-LP custody, non-DETF valuation, funding/vesting, access, collector rotation, and deployment mismatch assertions.
- Frontend `npm run check` passed lint/typecheck and 410 tests across 59 files; `reserve-policy-frontend-check.log`.
- Latest completed Solidity checkpoint: V3 ordinary/SY + imports 15 pass, Aave 9 pass. Dual suites had setup artifact failures after scope consolidation; explicit hook-flags artifact dependency restored. See `v3-green-dual-scope-artifact-*` and `dual-after-scope-artifact-trace.log`.
- Remaining identified follow-up: launch-script policy propagation (including premine arguments), further LP ownership/epoch/minimum-output coverage, current run failures if any, V3 query-facet EIP-170 size, remaining approved SE adapters and full-repository validation. No stage or full refactor completion is claimed.

## V4 runtime checkpoint after scope revision

`v4-policy-first-runtime-*`: build passed in 936.170 seconds; tests 115 pass / 20 fail in 27.919 seconds. The twenty failures all stop at actual protocol-fee LP creation in the Weighted/Curve fixture; investigation continues. CP/Orbital policy, LP-payment and collector cases and the selected Dual/binding cases passed. No full release completion is claimed.

## V4 policy and V3 size follow-up

`v4-policy-173-pass-six-epoch-*`: 347.012-second successful build; 173 pass / 6 fail in 23.589 seconds. All previously failing fee-LP cases pass after the fixture funds actual native SE inventory. All six new failures concern positive expansion setup: only the lead payment received yield, while multi-leg reserves choose their numeraire from their sorted pair list. The revised fixture funds each non-DETF SE leg; validation pending. V3 query facet runtimes are 21,871 and 18,071 bytes; the installed-facet size assertion passed.

Applied approved V4 position import conversion and native SY, with consolidated real-NFT, fee, sleeve, activation and standard-route tests in the existing FullRangeBook suite. Not yet validated. No Slipstream files are changed by this implementation.

## Current validation work

V4 native-position code builds, but its first runtime attempt stopped at a missing TWAP facet artifact. The gold TestBase now declares the factory artifact dependency. Its query facet is being split across the existing query facets to meet EIP-170. Two Orbital LP catch-up quotes also exposed use of stored sphere geometry with new effective SE balances; quote geometry now derives from the same live balances using the existing formula. Session 16998 validates these changes. Slipstream remains deferred, with no open product question or release gate.

## V4 position and liquidity checkpoint — 158 passing

`v4-position-158-pass-two-final-*`: build passed in 541.362 seconds; 158 tests passed and two failed in 29.671 seconds. The complete selected V4 liquidity-policy, Fee Collector, own-LP bond and positive-expansion matrix passed for all four bindings in both policies. Real V4 NFT import/fees/conservation and native SY money operations passed. Remaining failures were the obsolete one-token activation in the sleeve fuzz fixture and an oversized V4 OutFacet. The fixture now starts with funded two-token liquidity and independently verifies exact-output sleeve ceiling/claim conservation. OutFacet reuses the installed preview facet instead of embedding another copy of the quoter. V4 query facets themselves now fit (23,906 / 19,269 bytes).

Dual native SY and its shared standard liquidity routes are now applied, with four added cases consolidated into its existing production-path suite. Orbital's single-token split quote now also derives the sphere parameter from its live effective balances. Session 96760 validates these follow-ups. Full release validation remains incomplete; no Slipstream work is included.

## V4 native position complete selected validation / Dual preview correction

`v4-dual-sy-216-pass-one-route-*`: build passed in 480.592 seconds; **216 passed / one failed** in 30.980 seconds. All 24 V4 FullRangeBook tests pass, including actual imported NFT conversion, earned fees, native SY, first activation, sleeve fuzz cases and every installed facet runtime limit. The entire selected reserve permissions / own-LP payment / collector rotation / expansion matrix remains green. The one Dual SY route failure identified sequential fee dilution: a swap changes both SE books before the join, while the legacy preview used their starting states again.

The correction adds a sequential Dual quote library and a read-only `ReceiveShares` transition for already-issued share receipts. The latter changes only projected holder inventory; supply, backing, fees and pool state stay fixed. This permits share-input unwrapping to be included before quoting protocol fees and the zap. Existing operation enum values and selectors are preserved. ERC4626, V2, V3, V4, Morpho Blue and the funded ERC4626 test fixture implement the operation. No Slipstream file changes are involved. Session 12236 validates the implementation and a real-transfer state comparison; these latest changes are not yet claimed passing.

## Dual native SY selected validation — 218 passing

`v4-dual-sequential-sy-green-*`: successful build in 525.959 seconds, **218 tests passed / zero failed** across 25 suites (45.006 seconds for the test command; Forge suite summary 38.29 seconds). All four declared Dual SY input routes match their previews and selected raw/SE output payouts, with actual received-share state matching and installed facet size checks. The retained V4 position, V3, V4 reserve-policy/bond/collector matrices pass in the same selection.

Orbital native SY, actual exact-input single-output LP exits, sequential exit quotes and the exact-output unwrap balance check are now applied (`orbital-native-sy-sources.json`). Tests are consolidated into the retained SeBufferAbi suite, alongside retained lifecycle, permission, fee and swap cases. The first compile failed on an imported TestBase alias collision in the compilation root (8.977 seconds); selective contract imports resolve that namespace issue. Session 19383 validates the current source. No whole-release completion is claimed. The old exact-output LP-exit stubs remain unsupported; Pendle redemption uses the implemented exact-input route.


## Orbital follow-up and restricted SY caller preservation

`orbital-native-sy-first-runtime-*`: successful 397.308-second build; **239 pass / 16 fail** in 52.390 seconds. Fifteen failures exposed an unintended replacement of Orbital reserve valuation with an executable single-output exit quote. The retained proportional-plus-converted-residual valuation has been restored; the native SY exit quote remains separate. One SE-share deposit failed because the initial share receipt/redemption changes the underlying SE book before the retained zap formula runs. Its preview now projects that initial funded operation, then applies the existing split, fee and LP formulas. No issue is hidden by wider tolerances or disabled routes.

The native SY internal-balance path now retains the initiating caller in transient context across its self-call. Restricted hooks check that caller against the actual hook owner/current oracle Fee Collector, while additions keep their separate restriction. Three existing restricted SY suites now test unauthorized callers, authorized internal redemption and context cleanup. The existing sequential quote assertion additionally compares received-share projection with a real share transfer in the V2, ERC4626 and Morpho suites; V3 adds the same production-state comparison. Session 14447 builds and validates this batch. No passing result is claimed yet.

D66 remains unchanged: all 52 preserved Slipstream files match their scope-checkpoint hashes. No unfinished Slipstream functionality or validation is a release gate.


## Orbital and restricted SY follow-up — 288 passing

`v4-orbital-and-restricted-sy-288-pass-*`: build passed in **699.543 seconds**; **288 tests passed / two setup failures** across 41 suites (137.872-second test command, 121.28-second Forge summary). All sixteen earlier Orbital failures are resolved, including every declared native SY input/output route. Restricted internal-balance SY redemption and caller-context cleanup pass in CP, Weighted and Curve. V3 received-share state comparison and the retained V4 full-range, four-family policy/LP/collector/expansion selections pass. Orbital DepositQuery/Withdraw facets are 22,542/21,410 bytes; the sequential quote library is 12,930 bytes.

Both setup failures were missing artifacts in the new transition test roots: V2 InFacet and Morpho ERC4626Facet (`received-share-missing-artifacts-trace.log`). Their gold TestBases now import all factory-loaded family components explicitly. The existing CP SE-share deposit route is added to native SY input discovery with projected receipt/redemption before the retained pair-side zap. Output discovery remains the supported raw/pair directions. A shared actual-fee-LP test now checks native SY internal-balance removal across collector rotation in all four bindings and both policies. Session 86174 validates this next batch; these latest changes are not yet claimed passing.


## V4 native SY and collector checkpoint — 300 passing

`v4-native-sy-and-collector-300-green-*`: successful **313.968-second build**, followed by **300 passing tests / zero failures across 41 suites** (70.670-second test command, 65.04-second Forge summary). CP's third, SE-share input matches its native SY/standard preview and actual redemption; output discovery remains directional. The four-family/two-policy actual-fee-LP internal-SY collector-rotation cases all pass. V2 and Morpho transition suites pass after the artifact dependencies are included, with real received-share state checks. Orbital routes, restricted caller context, V3/V4 full-range/SY and the retained funded/policy/LP-payment matrices remain green. The prior CP legacy-codegen stack error (28.977-second build, `cp-share-input-stack-failure-*`) was resolved by splitting the arithmetic helper without changing compiler settings.

Next: the existing direct-liquidity test gains an authorized LP-transfer/spender case, then broader repository build/test migration and remaining in-scope integration checks continue. No full-repository pass or release completion is claimed. Slipstream remains explicitly deferred and unchanged.


## Authorized external LP ownership — 82 passing

`v4-lp-approved-spender-82-green-*`: successful 342.112-second build; **82 tests passed / zero failures** across all eight four-family/two-policy reserve suites (35.610-second command, 25.65-second Forge summary). The existing direct-liquidity test now transfers actual externally held LP using a finite ERC20 allowance. The approved spender can redeem its acquired LP in public mode, with actual payout/preview parity; restricted mode continues rejecting direct redemption despite legitimate LP ownership. No duplicate family fixture or separate new test suite was added.

The next check is the full default source/test/script build in the main directory. Its result is separate from the passing selected checks; legacy caller migration and full-release validation remain incomplete.

### Default repository build after the 82-test LP-spender checkpoint

Four full-build attempts surfaced unresolved legacy fixture compilation. Fixed undefined policy-argument variables in the V4 CP policy helpers and hostile Quad helper, and one removed deployment helper call in the excluded Balancer mixed decimal fixture (compilation maintenance only). The latest attempt exited after 154.658 seconds on missing legacy helper names in the excluded Balancer multi-vault bonding tests. Archived all failure logs; no full-build pass is claimed. Those older tests also reference removed legacy public APIs; their financial migration has not been resumed. Continuing approved V4 policy test/API migration. Slipstream remains unchanged.

### V4 release packaging validation

All ten current packaging tests passed after a 157.998-second build and 12.859-second test command. The manager-deployed package routes 48 product selectors once, retired standalone mint/burn/compound/close selectors are absent, remaining callback authorization and bytecode limits pass, and the actual Robinhood launch stage resolves current dependencies and rejects stale ones. No deployment was broadcast. Evidence: `v4-release-packaging-10-green-*`.

### Standard exchange fallback across four V4 reserves

All 108 tests passed across nine suites (build 517.541 seconds; test command 77.720 seconds, Forge summary 57.75 seconds). The existing shared reserve fixtures now exercise both mint and burn fallback, before and after real funded yield produces due eight-hour catchup. Exact-output minimum failures roll back payment and expansion; successful swaps preserve supply except for the precisely funded pending expansion and preserve protocol LP. The eight family/policy combinations use the same assertions and reuse the existing LP-bond yield helper. Ten packaging checks are included. Evidence: `v4-funded-fallback-and-packaging-108-green-*`. This selection overlaps the earlier 300/82 checkpoints and is not added to those counts. Legacy policy test migration and full default compilation remain incomplete.

### Orbital with actual V4 position-vault backing

Nine tests passed, including all ten combinations of two SE-share inputs and five advertised native SY outputs, over two distinct real V4 full-range vaults. Each vault is activated with both funded tokens through its standard multi-input route; the first DETF bond remains separately funded. Existing funded principal/vesting/rewards/static SY and installed-component assertions are reused. Build 109.618 seconds, test command 27.548 seconds, evidence `orbital-v4-position-binding-9-green-*`. Declared factory artifact dependencies in the existing production-SE test deployment library after the initial TWAP artifact setup failure. The combined ten-route test reports high aggregate gas; separate per-operation measurements are being added before gas suitability is assessed.

### Measured V4 position-backed native SY gas

The green ten-route matrix now measures each actual execution call separately (excluding preceding previews and transaction intrinsic gas). Deposits cost 58.6–65.0 million gas; payment-token/SE-share redemptions cost 33–34.3 million, and DETF output about 4.24 million. Evidence: `orbital-v4-position-gas-before-*` and structured `orbital-v4-position-gas-before.json`. These are warm-test execution measurements, not a live-chain gas-limit claim. Current work compares the existing state-based withdrawal quote against the canonical standard quote for identical shares and lower repeated-read cost. No financial formula or production runtime change has been applied for this finding yet.


### V4 exact-output quote reuse under validation

The standard/state quote comparison passed with exactly equal required shares for both actual V4 vaults; measured query gas was 4.70/5.29M versus 2.46/3.09M. Evidence: `orbital-v4-position-quote-comparison-*`. The shared V4 common target now owns the existing inventory withdrawal math so standard exact-output queries can reuse a single snapshot. Execution may reuse its supplied budget only after verifying the existing buffer and both adjacent exact-input quotes; arbitrary larger budgets still use the canonical inverse. Existing hooked/imported/multi-position quote paths remain available. The consolidated withdrawal test now compares the cached inverse against the unchanged exact-input surface, tests insufficient-budget rollback and exact/excess/full-budget execution with prepaid and pull settlement. Session 41588 validates this batch alongside all full-range and actual Orbital position-backed routes. No passing result for this latest batch is claimed yet.


### Cached V4 quote functional result and deployment-size follow-up

`v4-cached-quote-42-pass-size-failure-*`: build passed in 438.296 seconds; 42 tests passed and one EIP-170 test failed (22.623-second test command). All exact/excess/insufficient withdrawal budgets, prepaid/pull refund assertions, independent exact-input inverse comparisons, V4 import/sleeve checks and ten real Orbital SY combinations passed. Deposits fell to 44.0–45.8M execution gas and non-DETF redemptions to 14.4–15.2M; these warm measurements do not yet prove release gas suitability.

The cached math enlarged OutQueryFacet to 27,361 bytes and OutExecutionDelegate to 26,507 bytes. SY selectors are being moved to the existing smaller OutMultiQueryFacet without changing the proxy ABI. Execution now verifies share-budget hints through installed exact-input queries and falls back to the installed standard inverse, avoiding a duplicate embedded quoter. The existing size test also checks both CREATE3 execution delegates. The shared inverse additionally permits at most eight interpolation probes within verified forward-quote bounds before reverting to ordinary bisection; the target minimum and buffer remain unchanged. Session 83968 validates these follow-ups; no passing result for the latest source is claimed yet.


### V4 exact withdrawal optimization — 43 passing

`v4-cached-exact-withdrawal-43-green-*`: build passed in 151.256 seconds and all 43 tests passed in 15.381 seconds across three suites. The unchanged exact-input surface independently confirms required withdrawal shares, including small/fuzz amounts, both assets, exact/excess/full budgets, insufficient-budget rollback and prepaid refunds. Full-range/import/fees/lock/SY behavior, actual Orbital V4 routes and packaging pass. InQuery/OutQuery/OutMultiQuery runtimes are 24,225/22,202/15,821 bytes; both execution delegates fit, including Out at 14,755 bytes.

Standard exact-output quotes now measure 0.21/0.25M gas versus the original 4.70/5.29M for the two actual V4 vaults. Warm Orbital SY deposit calls remain 32.9–33.3M gas; non-DETF redemption calls are 6.05–6.40M and DETF output about 4.24M. `orbital-v4-position-gas-after-cached-withdrawal.json` contains route measurements and limits. Further deposit profiling and live-chain suitability remain open engineering work. Session 8813 runs the broader V4/collector/funded/SY selection, with the existing OutMultiQuery facet metadata test updated for the relocated selectors.


### Broad V4/SY/collector regression — 340 passing

`v4-cached-withdrawal-and-collector-340-green-*`: successful 37.678-second build and **340 passing tests / zero failures across 44 suites** (79.889-second test command; 74.43-second Forge summary). Includes the prior 300-test selection, subsequently added funded fallback/approved-LP-spender cases, real V4 Orbital binding, release packaging and relocated native SY facet metadata. The selections overlap and their counts must not be added. All 52 preserved Slipstream files still match the D66 checkpoint. No full build/test or release completion is claimed, and no Forge process remains active.

### Resumed full-repository compilation maintenance

Owner requested execution through completion. The next default builds exposed surviving excluded Balancer test callers whose helpers and package fields had been removed before D60. Compilation-only compatibility now retains those original setup signatures and test-only legacy interface declarations; no retired production selectors or Balancer economic behavior are restored. Real SY and bonding packages are supplied by the existing FactoryServices in the decimal fixtures. Existing legacy assertions remain intact.

Evidence: `excluded-multi-compile-compatibility.json`, `excluded-single-compile-compatibility.json`, and `excluded-decimal-package-compile-compatibility.json`. Archived failed build diagnostics are `main-full-build-excluded-multi-fixture-failure.*`, `main-full-build-excluded-single-helper-failure.*`, and `main-full-build-excluded-package-args-failure.*`. Another default full build is in progress; these are compilation repairs, not claims of runtime validation or full-plan completion. V4 policy migration, integration, consolidation and final acceptance evidence continue afterward.

### Robinhood transaction gas limit verified

Read-only mainnet RPC at block `0x368d763` returned `getMaxTxGasLimit() = 32,000,000`; `getGasAccountingParams()` also reported 32,000,000 for the block accounting limit. The reported Ethereum-compatible block-header `gasLimit` is not the transaction limit. No transaction was sent. Evidence: `robinhood-live-gas-parameters.json` and the credential-safe read script. The current measured V4-position-backed Orbital SY deposits (32.9–33.3M gas) exceed this limit, so deposit optimization and an executable bounded-gas regression are required before completion. This resolves the previously unknown live-chain suitability constraint; it is an implementation defect to fix, not an owner product question.

### Orbital buffered-input inversion — 78 passing

The current real V4-backed Orbital SY deposit now uses 12,504,682–13,298,441 measured execution gas (previously 32.9–33.3M) and succeeds with an explicit 31M call budget, below the observed Robinhood 32M transaction limit. Redemptions remain 4,237,472–6,398,563. The original exact minimum-input search now tries up to eight interpolated candidates inside forward-verified bounds, then falls back to binary search. An independent integer lower-bound reference matched all 19,185 generated curve cases. Economic formulas, approval limits and storage are unchanged.

All 78 selected production-path tests passed across ten suites after a 525.563-second build and 20.562-second test command: `orbital-buffer-inversion-78-green-*` and `orbital-v4-position-gas-after-buffer-inversion.json`. Additional real-V4 amount/input fuzz and retained-LP bond tests are now included in a broader regression. They enforce the same 31M call budget for deposit, redemption and bond purchase. This does not complete the full repository build, legacy test migration or remaining plan gates.

## V4 legacy test migration resumed

The default full build reached the V4 adversarial TestBase after 69.294 seconds; its first error was the retired mint alias. Equivalent mint/burn calls now use IStandardExchangeIn with the explicit raw-DETF endpoint: 317 calls in 93 existing test/helper files, recorded individually in v4-standard-test-call-migration.json. Original economic assertions are retained at this mechanical migration step and still need semantic review.

Both V4 adversarial base hierarchies now use owner-authorized funded NFT claims and exact held-DETF unstaking for their disabled-instance exit checks. Four duplicated F1 bodies now use shared current funding-authority checks and inspect the actual proxy loupe for removed sale selectors and absent diamondCut. The old malformed diamondCut calldata checks were insufficient to establish an absent upgrade surface. An initial focused compile found stack pressure in the expanded claim assertion; reusing the common claim helper addresses it, with validation currently running. No existing adversarial test has been removed.


### Funded reward custody and migrated CP policy — 376 passing

`v4-bookkeeping-policy-376-green-*`: build passed in 373.279 seconds; 376 tests passed, zero failed, across 50 suites (83.384-second command; 78.44-second Forge summary). Reward synchronization now records observed held-token reserves after settlement, without joining LP. All five previously failing TrustFlags pretransfer cases pass with explicit reserve/balance assertions. The retained 343 V4/SY/collector tests, 23 migrated adversarial tests, and ten CP policy tests pass together. Policy coverage includes funded primary/swap selection, explicit versus implicit expansion, failed-minimum rollback, and a 30-year uncapped catch-up that closes the primary mint gate. No full repository build or release completion is claimed.

### Provider and n-leg legacy migration in progress

Seventy-six remaining gas-option mint/burn calls now use the standard interface with identical 30M limits (52 execution calls and 24 previews). The two gold TestBase hierarchies share a funded mature-claim/unstake assertion reused by 50 existing provider lifecycle files; their first-bond, mint/burn, provider-specific setup, and existing allowance/custody diagnostics are retained. The three duplicate CP/V3 funded wrapper tests are consolidated into their original lifecycle tests by activating both SE assets in the canonical fixture. Weighted/Quad policy overrides now reuse common funded assertions, retaining Weighted's distinct simultaneous A-primary/B-fallback case. These newest changes are under focused build/test validation and are not yet claimed passing. Full compilation most recently stopped on the now-replaced Quad provider mature-close projection helper (`main-full-build-v4-retired-close-preview-failure.*`, 102.864 seconds).

### Funded principal and provider migration — 44 passing

`v4-funded-principal-provider-44-green-*`: build passed in 440.468 seconds; 44 tests passed, zero failed, across seven suites (17.875-second command; 12.59-second Forge summary). All four actual reserve bindings now pass the consolidated funded-principal, donation isolation, mature sDETF claim, exact staking and rollback cases. CP native-unit boundaries, five actual CP/V3 provider cases, and Weighted/Quad policy cases also pass. The OriginalPrincipal suite consolidates 33 legacy executable cases into 18 current cases with explicit decision mapping; CP/V3 consolidates three duplicate wrappers into the existing lifecycle fixture. Full repository build/test and release acceptance remain incomplete.

### Funded claim and proxy controls — 53 passing

`v4-funded-d25-surface-53-green-*`: build passed in 128.791 seconds; 53 tests passed, zero failed, across 14 suites (18.369-second command; 10.22-second Forge summary). Shared current DETF selector/loupe controls pass on CP, Weighted, Orbital and Quad. Quad also checks the exact split hook execution/query/SY surfaces and absence of retired LP-claim projections. D25 now has four shared funded cases per fixture instead of eight LP-close cases, passing on CP, Quad and all eight retained native-decimal combinations. The Pons D25 consumers were migrated to the same shared assertions but their remaining legacy helpers still need migration and validation. No full repository pass is claimed.

### Standing funded income and mature-claim security — 29 passing

`v4-funded-fees-and-claims-29-green-*`: build passed in 88.281 seconds; 29 tests passed, zero failed, across five suites (6.347-second command; 2.56-second Forge summary). All four reserve bindings pass six shared fee/creator cases: independent two-wave integer allocation, exact full exit and later receipts after oracle rotation, no duplicate funding, reserved-role rejection, zero-creator fallback and distinct explicit creator. Five mature-claim authority, recipient, inventory-isolation and exact unstaking cases also pass. The fee suite replaces twelve repeated cases per provider with six inherited cases and reuses the existing independent FundedRewardAssertions reference; 58 caller/base files are mapped. Provider/native variants beyond the four selected bindings remain to validate. The default build last failed after 294.378 seconds on an external TokenStaking test calling retired mint; that active production caller is now migrating to separate DETF purchase and sDETF staking endpoints.

### Actual TokenStaking/Pons integration — 32 green

`token-staking-real-pons-32-green-run.json` records 32 passed / 0 failed in two actual suites. Build 89.764 s; test command 11.368 s (Forge 4.54 s). The Pons fixture now funds two-token SE activation before DETF deployment. Actual migration uses Standard Exchange for raw DETF purchase and exact held-DETF staking; approval cleanup, slippage rollback, funded growth, chunking, owner checks and withdrawals passed. This focused evidence is not a full default build or release completion.

## Native pair donation follow-up — 31 passing

`v4-native-pair-donation-31-green-*` records a successful 93-file build (602.861 seconds) and 31 passing tests across five suites (7.556 seconds command, 2.72 seconds Forge). Native pair donations now use the reserve's existing direct pair join and matching preview; actual held SE-share donations retain their validated share route. This includes current SAF child interfaces, primary ordinary mint and Quad funded claims. The preceding 39-pass/6-failure selection remains in `v4-mint-donation-first-runtime-*`. The separate SE-share mint fallback preview mismatch remains an implementation defect under investigation; its exact-equality regression is retained. These selections overlap earlier evidence and do not establish full-release completion.

## Composed share fallback and Pons migration — validation in progress

The strict T7.7 regression exposed an SE-share redemption changing the reserve's buffered claims before its fallback swap. The new `IDetfReserveQuote.previewSwapAfterRedeem` projects that redemption and reuses existing CP, Weighted, Orbital and CurveQuad curves. Tests compare the quote with an actual redemption followed by the reserve quote on a restored snapshot; eight real family/policy bindings also check slippage rollback, custody and supply. Native-pair donation remains verified by the 31-pass checkpoint above. This composed-share change is **not yet green**. Its first compile failure was an incorrect donation-interface import (16.116 seconds), followed by excess test locals (681.632 seconds); both were corrected. The next compile exposed a retired Pons synchronization helper (84.811 seconds), now migrated to funded reward synchronization. All three logs remain preserved.

The Pons policy and product-law callers now use standard funded payouts. The gold, native-decimal and Pons policy suites share one assertion behavior; Pons no longer fabricates DETF to move policy prices. Linear principal claims, funded sDETF rewards during vesting, complete mature payout, and current standard unstaking replace LP-sale expectations. Their source mappings are in `shared-funded-policy-migration.json` and `pons-product-funded-migration.json`; runtime validation is pending. `pending-composed-exchange-followups.json` records further composition/provider checks, not owner product questions.

The local rehearsal migration is applied but not executed. The expected Anvil endpoint at port 18663 is currently stopped: an authorized read-only chain-ID probe returned connection refused. No deployment or broadcast was performed.


## Composed SE-share fallback validated; Pons follow-up remains

`v4-share-composition-192-pass-pons-failures-*` built 224 files successfully in 1,184.206 seconds (solc 1,173.51), then ran 204 tests across 11 suites in 61.099 seconds (Forge 54.95): **192 passed / 12 failed**. All eight reserve-policy suites and all fourteen I/O cases pass, including independent real-redemption snapshot quotes, exact T7.7 parity, minimum rollback and whole externally owned LP donations. This is a successful selected regression inside a mixed-result run, not a green Pons or full-release claim. All four new quote facets fit EIP-170: CP 24,486; Weighted 21,396; CurveQuad 22,275; Orbital 23,702 bytes. CP has only 90 bytes of headroom.

The twelve Pons failures expose stale test assumptions and actual provider composition defects. The pinned Pons V2 hook charges separate floored hook-fee and creator-tax cuts on the swap's unspecified leg; the V4 SE quoter previously omitted those cuts and rejects its transition state simply because a pool hook exists. Pons donation/primary burn quote parity therefore remains open. Test fixes are prepared for typed NFT-position retirement checks, preserving earlier staking receipts while acquiring raw DETF for price movement, actual self-leg funding for a locked-manager donation, and face-only prepayment-surplus accounting. The failing assertions remain preserved.

The next validation batch applies the shared D15 funded-unstake behavior across four reserves and eight native-unit variants, actual NFT-parent deployment tests, standard hostile-callback purchase, and native dust/donation callers. Unconsumed internal mint preview LP/share computations were removed without changing the external interface or economics. DN3/DN15 return-only placeholders are retired with explicit mapping to actual eight-binding LP donation and fixed funded-principal conservation. No Slipstream source was edited; the separate A30 approval-review block remains unchanged.


### Funded unstake first runtime and Pons fee correction

`v4-funded-unstake-first-runtime-*` records a 1,603.222-second successful build and **244 passed / 31 failed across 41 suites** (67.758-second test command). All selected I/O, reserve liquidity, native dust/donation and hostile callback cases pass. Remaining D15 failures trace to oversized Weighted later-bond fixtures, prior staking receipts consumed by raw-price setup, obsolete Orbital eighteen-decimal setup, and a Quad fixture without a premium. The NFT absence assertion now uses the typed position view. `v4-funded-unstake-fixture-followups.json` maps these changes and consolidation of duplicate D15-9 into the stronger D22 assertion.

The known Pons fee correction and actual root/native SE tests are applied and under build/test validation. Quotes read the existing per-pool immutable terms, include independently floored hook/creator cuts on the unspecified swap leg, and support projected state for the pinned Pons hook. Tests include actual two-token activation, both swap directions, exact-output budgeting, failed-minimum rollback and future-policy changes. No result for this latest batch is claimed yet; EIP-170 sizes remain to check.


### Pons and funded unstaking — functional regression green, size follow-up

`pons-and-funded-unstake-233-pass-size-failure-*` records a 714.432-second successful build and **233 passing functional cases / one EIP-170 failure across 22 suites** (102.768-second command). All twelve prior Pons failures and all D15 fixture failures are resolved in this selection. All three actual Pons SE pool suites pass with native units, nonzero fee/tax, frozen pool terms and exact input/output minima. The V4 transition-query facet grew to 25,281 bytes; raw-word decoding of the pinned hook metadata is applied and awaits size validation.

The owner-approved mature-close cleanup now removes two proxy getters and five unused unified V4 fields. Product/query selector counts become 46/35. Registry processing, salt calculation and initialization reject noncanonical payloads; frontend types, encoding and callers use the reduced payload. Removed-field tests are mapped to current payload/surface controls, while real funded claim assertions remain. `approved-v4-close-cleanup.json` records the current patch, hashes and coverage mapping. The new selection includes all 29 proxy suites for 25 SE share issuers plus V4/Pons/collector/cleanup regressions. Full default build/hermetic evidence and remaining composition work are still required.


### All native SE issuers and approved cleanup — 603 passing, three size failures

`all-native-se-and-approved-cleanup-603-pass-size-failures-*` records a successful 188-file build in 1,725.623 seconds (solc 1,698.94) and 606 tests across 67 suites: **603 passed / three EIP-170 failures**. Test command 456.862 seconds; Forge runtime 443.53. All 29 mapped proxy suites for 25 SE issuers ran. The failed metadata/deployment checks identify V4 InQuery 25,124 bytes, CurveQuad SE 25,944 and Weighted SE 25,422. Their transaction cases pass, but these packages cannot be considered release-ready until their deployed facets fit the limit. The two hook SE facets inherit the newly added hook-only composed quote despite not exporting it; separating that target code preserves their actual proxy API. The V4 snapshot can move unchanged to an existing smaller query facet.

The owner-approved obsolete close cleanup passes fresh ABI/schema/source checks (`approved-close-cleanup-surface.json`) and selected actual deployment, retired-selector, funded-claim and caller tests. Frontend lint/typecheck and all 410 tests in 59 files also pass (`frontend-approved-close-cleanup-check.json`). This resolves the earlier cleanup approval and its selected validation; no owner approval remains pending. Broader acceptance/full repository validation continues.

The same-source warmed policy fixture baseline passed 14 tests across two suites (9.498-second skipped build; 15.020-second test command, 6.82 seconds Forge). Only the existing fixture candidate ordering is now changed: try the already-observed successful 2.2-WAD opening first, retain every former candidate as fallback. The identical after-selection is running. No production source, fuzz count, gas budget or assertion changed between the two measurements.

Remaining prepared work includes exact external SE conversion projections and actual provider regressions; immutable provider-dependent rate composition and ordinary opaque ERC4626 compatibility remain engineering work. A concrete launch gap also remains: the two existing Robinhood testnet SE helpers seed the pool but do not activate the SE with both tokens. Its shared helper and V3/launch test migrations are prepared; no deployment/broadcast occurred. D60 and D66 remain unchanged; all 52 preserved Slipstream files still match their recorded hashes.

### 2026-09-08T04:24:50.283422+00:00 — continued integration and fixture comparison

The 14-case policy fixture comparison passed before and after. The two expansion setup cases reduced EVM gas by 95.65% and 95.25%; twelve other cases are unchanged. Forge suite time was 6.82s versus 6.52s, while whole command time increased from 15.020s to 27.624s. No full-suite speedup is claimed. See `policy-fixture-performance-comparison.json`.

Applied current-source query relocations, final dead-error/no-op cleanup, dual-token launch activation and its actual-path tests, four-provider external-input transition quotes and selector wiring, and generalized four-host reserve quote composition. The CP query now uses an existing smaller facet. Build/test session 68339 is running; source is frozen until exit. These changes are not yet validated. Provider-dependent rates, opaque ERC4626 composition, custom donation previews, full repository checks and final acceptance remain required. D60/D66 are unchanged; owner-approved mature-close cleanup is resolved and selected validation passed.

### 2026-09-08T04:50:32.059659+00:00 — approved dead target removal and quote validation

Removed the unreferenced `UniswapV4DetfBondNFTVaultTarget.sol`; the deployed V4 NFT facet already inherits the common funded implementation. This completes another part of the explicitly approved obsolete mature-close code cleanup without changing funded claims. The generic legacy NFT implementation needed by excluded Balancer DETFs is retained. Validation is pending for this additional deletion.

The latest combined build stopped on a rate-helper stack-depth error. The fix preserves arithmetic and reduces live locals/arguments; a selected three-provider-facet compile is now used to catch these errors before another larger build. Compiler profile, optimization and fuzz settings remain unchanged. Prepared MetaMorpho, custom-donation and V3 legacy range cases are applied and await runtime validation.

### 2026-09-08T05:06:49.317379+00:00 — current facet sizes and combined build

The latest selected compile passes: V4 OutQuery 23,383 bytes; OutMultiQuery 15,821; Orbital Hooks 24,516; Orbital DepositQuery 23,442. Unchanged proxy functionality is split across existing facets; no new deployed facet/package was introduced. The combined artifact build/test is running in session 72664 (2,081 reported compilation units). Narrow leaf compilation was useful for compiler diagnostics but preceded a wider cache rebuild; no overall build-time improvement is claimed. Future leaf checks use separately seeded compiler-cache metadata, preserving canonical out/ artifacts and the primary broad cache. The running compiler will not be interrupted.

### Current combined validation and follow-ups — 2026-09-08

Session 72664 remains in its 2,081-file Solc 0.8.35 build; no Solidity source is changed during that process. The explicitly approved V4 close cleanup remains applied. While it runs, prepared (not applied) follow-ups cover exact epoch-boundary stake participation, a custom fee-bearing bond-payment preview, and the fourth V4 fork binding with current Fee Collector deployment and correct predicted-token decimals. The deployment review also found fixed CREATE3 namespaces on the changed collector manager and SE rate providers; `prepare-current-provider-deployment-salts.py` applies the existing release-salt convention after validation exits. These are implementation issues, not owner questions.

The local rehearsal endpoint is stopped. A reviewed read-only archive probe successfully retrieved 24,009 bytes of actual PoolManager code at the saved Robinhood block; the fork environment is available once the local node is started. `acceptance-test-anchors.json` provides 67 named source entry points for 37 criteria without asserting execution success. A prepared cache seed retains current records and can add only absent baseline entries with unchanged complete import closures; it has not mutated the active cache. No Slipstream or Balancer-hosted DETF functional edit was made.

### Current composed route checkpoint — 2026-09-08

Combined default-profile build passed (2081 files,4828.584s). Selected integration run:351 passed,9 failed across41 suites (568.482s). All eight failures in funded V4 bindings are the new custom protocol-share donation preview; the ninth is missing runtime artifact discovery in the wrapped rate-provider setup. Real MetaMorpho pending-fee sequence, V3/V4 funded position/activation/import, reserve permissions/LP payments/current Fee Collector and selected native SY checks passed. Current V4 SE and composed hook runtime sizes pass EIP-170;10 approved cleanup surface checks pass. Evidence:`current-composed-quotes-351-pass-donation-artifact-failures-*`.

Donation traces identify two implementation issues: wrapper prepaid receipt tokens are counted in pre-mint backing; CP/Orbital donation previews treat newly minted SE shares as already-issued inventory. Direct prepaid and custom-bond regressions plus A22 epoch-entry coverage have been applied; their test-only checkpoint is running. Optional external deposit projection and real-provider tests remain prepared until that checkpoint exits. No production edits were made during the completed build.

An isolated local Robinhood archive fork is ready at localhost:18663,chain4663,block56118361, with24576-byte code and32000000 block gas limits. It uses a new visible artifact directory; previous saved rehearsal state/manifests remain untouched. No package deployment or public-chain mutation has occurred. Full plan completion and final default/hermetic/fork acceptance remain outstanding.

## Current custom payment composition and quote packaging

The direct custom-bond/prepaid-receipt checkpoint reproduced three failures while the A22 boundary case and wrapped-rate-provider artifact repair passed (`prepaid-and-custom-bond-reproductions-*`). Applied receipt accounting, external SE deposit projection, custom donation/bond projection and Orbital dependent-rate refresh now compile. The latest narrow check reports every selected facet and linked library below 24,576 bytes (`current-external-deposit-quote-leaf-sizes.json`). This is not execution parity evidence.

V2 query selectors are separated into `UniswapV2StandardExchangeQueryFacet`; the existing package now receives and installs that component explicitly. Existing V4 leg helpers share their view logic through a linked library, retaining current formulas and the proxy's storage context. Independent V2 and CP facet controls and a real funded Orbital rate-provider regression are included in the current consolidated build/test selection. The two preceding aggregate compile failures (wildcard import collision, then a test incorrectly marked view despite using Crane behaviors) are fixed and archived.

The strict-size local Robinhood node remains available on port 18663 at block 56118361. Rehearsal artifact discovery now includes Orbital, funded bond/SY components, Fee Collector, SE rate providers and the shared quote library. Package/lifecycle execution remains pending successful current validation. No Balancer-hosted DETF functional or Slipstream edit was made.


### Current composed quote checkpoint and follow-up

The integrated current-source build completed successfully (406 files, 1556.931s). Its selected runtime run passed 39 of 43 tests across 15 suites (15.890s command time). All CP, Weighted and Curve custom routes, direct prepaid receipt fixes, V2/V3/V4/ERC4626 external deposit projections, new V2 and CP declarations, A22 boundary ordering, and the real Orbital dependent-rate SY exit passed. Four Orbital bond/donation assertions failed after the quoted principal, reward backing, and main donation output had already matched: the retained residual sweep minted additional protocol LP beyond the primary payment join's return. The migrated tests now reconcile the complete custody delta against actual hook LP mint logs, and retain exact principal/fee/backing/preview checks. Donation tests also require the exact final minimum and atomic rollback at one raw LP unit above it. Runtime validation of these follow-ups is pending.

Applied the remaining orphan mature-close NatSpec cleanup and removed write-only V4 `epochAnchor`. `lastExpansionTimestamp` remains the actual first-bond-anchored eight-hour clock; excluded Balancer clock fields are untouched. Refreshed only the existing frontend V2 package ABI from the freshly compiled package, including its explicit query-facet dependency; current frontend checks are running.

A concrete source review found that existing Stata custom inputs would otherwise call missing optional composed-quote selectors. Added exact Stata-backed transition/external projections to the existing input target/facet and package declaration, preserving actual route economics and the current accepted deployment policy. Existing real Stata/SE proxy tests now reuse the shared transition assertions. This follow-up is compiling; other supported-provider compatibility still requires review. No owner decision is pending.


### Full current repository build started

The real Stata projection/native-SY suite passed all eight tests after a 144.243-second build and 12.784-second test command. Its current input facet is 12,695 bytes and package is 11,397 bytes. Added code-bound Stata component/package salts plus a real occupied-legacy-namespace regression before starting the complete default `forge build --offline` (no source/test/script filters). That full compile is now running; no Solidity files will change until it exits.

The completed Stata validation metadata was merged back into the broad warm cache. A further 209 absent cache records / 226 artifacts were safely seeded from the immutable baseline only where the entire source/import closure and compiler profile were unchanged. Current entries and all artifacts were preserved. The seed does not skip changed source or fabricate hashes. Full-suite timing remains measured, not inferred.

Frontend checks after the current V2 ABI refresh pass: lint, typecheck, 410 tests / 59 files, 58.748 seconds. The remaining local rehearsal and complete hermetic/baseline-attribution work remain required.

### Current full-build compatibility repairs and Orbital launch stage

The 15 compiler errors from the full repository build were addressed with existing legacy-interface casts, exact historical selector constants and restored virtual declarations. Balancer DETF edits are compilation maintenance only; Slipstream is unchanged. The main Robinhood workflow now stages the current Orbital hook package and the fork lifecycle reads that staged pin. The existing stage suite includes replay, registry and facet-size assertions. Shell syntax checks pass; full Solidity build and runtime validation remain pending.

The full-build continuation repaired inherited selector qualification in three historical Balancer surface tests and surviving-field initialization in four existing Composed harnesses. These are D60 compilation maintenance and do not restore retired production selectors or change excluded-family economics. The current full default build remains running. A Lido projection draft and associated existing-package wiring/tests are prepared for review only (`lido-composed-quotes.patch`); no Lido source changes are applied or validated yet.

### Lido provider projection checkpoint

The current Lido target/facet/package and code-bound FactoryService passed 8 production-proxy tests (395-file build 66.360 seconds; test command 1.983 seconds). This includes four existing native SY cases and four new complete-state/fee/external-conversion cases. The non-unit live Lido ratio was read successfully at Ethereum block 24,000,000; four real-protocol fork tests were added to the existing fork source but have not run. The V4 nested helper now approves only the acquired input and clears allowance after standard execution; the existing CP reserve suite adds Lido fallback and funded-bond regression coverage in both policies. Those V4 changes await validation. Current full default build still addresses excluded-family historical compilation references; no D60 functional migration or Slipstream edits.

The real Lido fork checkpoint passed all four tests at Ethereum block 24,000,000 (build 50.169 seconds; test command 9.027 seconds). This verifies actual fractional receipt/share rounding, changes to the staking ratio, both liquid/wrapped sequential books and external mint fee attribution; whole encoded projected states equal execution. No transactions were broadcast. The Lido In facet is 16,233 bytes and its package 9,775 bytes. V4 buffer integration and full repository acceptance remain pending.

Four V4 adversarial reentrancy fixtures (CP, Weighted, Orbital and CP decimal variants) now enter through standard exchange. Each funds and executes the exact nested bond call while unlocked before arming the callback, then checks the IsLocked selector and unspent nested payment. Runtime validation is pending. Two excluded Single-over-Composed matrix fixtures were made compilable by assigning their existing inputs without fabricating the missing funded components; their old runtime expectations remain excluded under D60.

### Current source and retirement mapping reconciliation

Refreshed all 67 named acceptance test anchors, with none still only prepared. The source-only consolidation comparison covers 3,111 files and records 6,497 versus 5,236 directly declared tests/invariants; inherited executable counts and full performance remain separate. Supplemented the retained regression mappings for 393 declarations across 55 sources. All in-scope files with retired declarations now link to a record; this is traceability, not a passing-runtime claim. The historical FoT placeholder contained only `return`; removing it did not remove an exercised assertion. Current short-delivery and trust-flag regressions remain. Two stale close-cleanup approval notes now point to the owner approval and applied 46-selector surface. The full default build remains active and Solidity source has not changed during it.


### EtherFi provider projection prepared during the full compile

Prepared a current-source change to the existing EtherFi input target, facet, package and FactoryService, plus five cases in its existing actual-protocol fork suite. The projection tracks global eETH shares, liquid/wrapped/queue balances, exact receipt rounding, soft staking, usage-fee issuance and instant-redemption fee/share burns. It follows execution rounding rather than the manager's differently rounded public preview. The source is not applied, compiled or validated; full build session 74210 remains active and Solidity is unchanged. See `etherfi-composed-quotes-prepared.json`.
