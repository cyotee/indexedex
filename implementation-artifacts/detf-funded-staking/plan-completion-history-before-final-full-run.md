## 12. Completion record

The full plan remains in progress. The main repository contains the implementation, preserved unrelated changes, and reviewable validation artifacts. No owner decision remains pending for mature-close cleanup, Fee Collector redemption, Balancer exclusion or Slipstream deferral. The explicit cleanup approval and its applied changes are recorded in `v4-close-cleanup-owner-approval.json`; the V4 storage audit maps all 30 retained fields, and the write-only `epochAnchor` is removed while `lastExpansionTimestamp` retains the first-bond epoch clock.

Current completed checkpoints:

- **Full pre-follow-up build:** the complete default contracts/tests plus all 294 maintained script sources passed in 23,391.542 seconds. Exact results and source provenance are preserved in `pre-provider-build-checkpoint/`. The earlier default-only 5,638-file build remains in `default-full-build-5638-green.*`. Seventy-four checked provider/position changes were applied after the latest successful build; a matching final full build and hermetic run remain required.
- **Funded artifact gate:** 16 records passed current compiler-metadata source-closure checks, with no stale imports and all deployable facets/packages within EIP-170. Rerun `reconcile-funded-selector-manifest.py --require-current` after subsequent Solidity changes.
- **Focused integration:** all 48 tests across 22 suites pass, including the corrected Orbital reentrancy fixture, custom bond/donation quotes and custody, Lido-backed V4 fallback/bonds in both policies, Stata deployment salt, and the main Orbital launch stage. The command took 293.278 seconds; `pre-provider-build-checkpoint/post-build-followups.json` and its log retain exact evidence. Earlier failed or empty selections remain historical records and are not validation evidence.
- **Native SE/provider evidence:** the earlier overlapping 603-pass selection covered 29 native-SE proxy suites for 25 in-scope issuers. Its three measured size failures were subsequently fixed. Stata has eight passing proxy projection tests; Lido has eight passing proxy tests and four passing actual-protocol fork cases at block 24,000,000. Camelot now passes 19 real-proxy cases, including sequential complete-state comparisons and five previously failing checkpoint/LP regressions. EtherFi passes all five actual-protocol projection cases at block 24,000,000; its five deployed components are within EIP-170. These focused results do not replace full current acceptance.
- **Frontend:** lint, typecheck and all 410 tests in 59 files passed the compiled V2 package ABI refresh in 58.748 seconds (`frontend-v2-query-package-check.json`).

The maintained deployment callers live under `scripts/foundry/`, while Foundry's default script root is `script/`. `run-full-build.py --include-scripts` keeps every contract and default test and adds 294 maintained/current script sources without changing compiler settings. Fourteen preserved research sources concern DualLiquidity, already deleted in commit `c0631349`; the alignment PRD explicitly says this product will not be repaired. Those files and 63 historical sources in `scripts/archive` are inventoried but are not active release callers. Actual script compile errors are being repaired and archived individually. Compilation and current-package local deployment remain separate gates. The subsequent unlocated stack error was isolated to five scripts using the same Orbital research JSON renderer; the other 289 roots passed diagnostic code generation. Its memory/concatenation repair and the Robinhood testnet token-decimal export migration are applied. The combined maintained-script rebuild passed all 294 roots, compiling 1,376 files in 602.173 seconds (`maintained-script-294-green.json`). `script-codegen-isolation.json` and the two applied preparation records retain exact evidence. These diagnostics do not complete release acceptance.

The strict local Robinhood fork is available for deployment of current packages and the full lifecycle rehearsal. Current-source complete hermetic validation, exact failure attribution, remaining composed-provider compatibility, actual local package deployment, and final acceptance reconciliation remain required. Source, configuration, closure and size checks supplement actual proxy and deployment tests; stale artifact ABIs cannot establish acceptance.

After the successful combined build and 48-test integration run, the guarded handoff applied all 74 checked provider/position source changes in the main repository. Exact before-file backups and both source fingerprints are preserved in `prepared-release-followups-applied.json` and `provider-position-followups-before/`. The changes cover Aerodrome fee compounding; retained V2/Camelot/Aerodrome LP-denominated routes; Rocket Pool registry/version accounting; ERC-4626 receipt custody and fee distinctions; removal of unread V3/V4 caches and never-created V4 wings; and 45 existing V3/V4 fixture migrations. No Balancer DETF functional or unfinished Slipstream work was added.

Every applied source had exact successful compiler-input coverage before application. All 26 V3/V4 production components fit EIP-170; the expanded fixture inventory typechecked 248 suites / 1,920 methods, and representative bytecode generation covered 44 sources / 356 methods. These diagnostics are not runtime acceptance. `run-provider-position-followups.py --label funded-followups` now runs sequential builds and actual tests: 80 local provider cases, the 1,920 retained V3/V4 methods, fourteen Rocket cases across two pinned protocol versions, and four actual sfrxETH cases. It stops on an unsuccessful or incomplete result. All 80 local provider cases now pass, with every built component inside EIP-170. The V3/V4 component build also passes. Its completed runtime batch executed 1,997 tests across 265 suites: 1,943 passed and 54 failed, with no missing required suite. The failures exposed stale decimal full-range/import expectations, old facet declarations, donation accounting that omitted the second activation payment, a raw-unit zero-share expectation, and TWAP event checks installed before setup token minting. Nine existing fixture sources are now corrected, with unchanged test counts and every decimal leaf retained; `position-runtime-migrations-applied.json` records exact changes and before-file copies. The same migration strengthens the E1 round trip to redeem every attacker share. All 333 methods across 46 affected suites now pass with zero failures or skips; the exact recheck took 925.280 seconds, including 770.22 seconds of compilation. All fourteen Rocket Pool actual-protocol cases now pass across pinned blocks 24,000,000 (v3) and 25,934,585 (v4), including collateral burns, deposit fees, queue assignment, custody, previews and component sizes. The sfrxETH fork and matching complete repository validation continue sequentially in `final-provider-repository-post-position-sequence.json`. The attempted shared-cache seed did not eliminate Aerodrome fixture compilation, so it supplies no measured speedup claim. The intermediate zero-execution hermetic deferral is retired, and the complete matching final build/hermetic run, baseline attribution and strict local deployment/lifecycle rehearsal remain required.

Current call-graph review identified three further unread V3 NFT metadata fields: imports drain the NFT and create a directly vault-owned full-range position, so no live accounting operation consumes the recorded manager, token ID or active flag. `v3-import-storage-followup-applied.json` records their applied removal using the existing center initializer after the successful affected-suite run. The same follow-up corrects both multi-exchange interface comments for native/WETH PoolKey ordering; the final complete build and hermetic run must validate the resulting current source snapshot. The earlier retention explanation is corrected in the storage disposition record. V4 import settlement context is separately used and remains. Nine co-located V3/V4 package documents now link to the accepted release law and identify conflicting historical activation, tick, share-math and selector instructions as superseded.

The immutable baseline is `e08b415733e729471779dcb8102529631330ea16`: 30,056 pass / 3,632 fail, with five documented misplaced network-source exclusions. The current compiled inventory has 2,699 concrete test contracts and 31,239 test/invariant methods versus baseline 3,073 and 36,563. In-scope/shared methods decrease from 29,085 to 27,529, while artifact size increases; excluded Balancer and deferred Slipstream counts are recorded separately. These counts are not runtime results or proof of a speedup. The identical 14-case fixture comparison passes before and after with roughly 95% less setup EVM gas in two cases, but command wall time increased. Full current hermetic timing is still needed.

Criterion status and evidence are in [acceptance progress](../../../implementation-artifacts/detf-funded-staking/acceptance-progress.json). `current-implementation-queue.json` and `execution-status.md` track current work. Detailed previous checkpoint prose is preserved in `plan-completion-history-before-script-reconciliation.md` and `execution-checkpoint-history-through-20260908.md`. Overlapping selected results must not be summed or treated as full-plan completion.

Applied follow-up validation also covers forty-five existing V3/V4 fixture sources whose activation still used consecutive single-token deposits. Their base and decimal cases retain subsequent single-token, locked sleeve, native ETH, imported-position, TWAP, nested-caller, secure-pull, booked-inventory and adversarial coverage after two-token initialization. Old package checks include the added SY and projection interfaces. Duplicate V4 first-activation execution/preview setups are consolidated into cases checking both outcomes. Native ETH multi routes follow PoolKey order with WETH as currency0's ERC20 face, consistent with `vaultTokens()`; the ordinary numeric-order check cannot reject that native mapping. The applied case map preserves every decimal leaf and maps 349 declarations to 345 with no unmapped retirement. Typechecking and bytecode generation are not runtime acceptance.

### 12.1 Revised remaining estimate — D66 scope

Planning estimate as of 2026-09-07: **3–5 working days of remaining implementation and validation**, including approximately **12–24 hours of build/test wall time** across focused iterations and final repository checks. This is an engineering estimate, not a release commitment. No unfinished Slipstream implementation or Slipstream-specific tests, forks or UI validation are included. Existing tests and completed evidence remain preserved.

Remaining work: complete V4 legacy test/API migration and consolidate redundant fixtures with named replacement coverage; repair excluded-family fixture compilation only as needed for the full repository build; validate real-provider integration and reduce the measured cost of V4-backed Orbital deposits; finish the in-scope package/selector/storage inventories and shared cleanup; run consolidated production-path, relevant fork/UI and repository checks with baseline failure attribution. The selected V4 position/import/native-SY, reserve-policy/LP-payment/Fee Collector and V3 facet-size checks now pass. Real V4-backed Orbital SY routes also pass functionally, but warm execution measurements initially exposed 58.6–65.0M-gas deposits and roughly 33–34.3M-gas non-DETF redemptions. Validated exact-withdrawal quote reuse now reduces them to 32.9–33.3M and 6.05–6.40M respectively; DETF output stays near 4.24M. Read-only Robinhood mainnet verification at block `0x368d763` reports a **32,000,000 transaction gas limit** (`robinhood-live-gas-parameters.json`). Subsequent verified input inversion and cached SE claim baselines reduce the ordinary deposit matrix to 11.45–12.14M. A new amount fuzz exposed a 31M-budget failure; that exact input is retained as a fixed regression and now uses 26.54M. All 343 selected regressions pass with actual deposit/redemption and retained-LP bond calls constrained to 31,000,000 gas (v4-cached-orbital-343-green-*; orbital-v4-position-gas-after-cached-claim.json). Finite fuzz coverage is not a universal gas bound. This is an implementation constraint, not an unresolved product decision. These tasks can overlap long compiler runs when they do not mutate sources being compiled.

Measured context: the first V4 policy build took 936.170 seconds (115 pass / 20 fail); those twenty fixture failures were resolved by funding native SE inventory. The next run built in 347.012 seconds (173 pass / 6 new catch-up setup failures). The subsequent position run built in 155.917 seconds (124 pass / 3 fail): missing TWAP artifact and two Orbital live-book quote failures. Those cases were corrected. The latest selected V4/V3/Dual/Orbital/Fee Collector and transition-quote batch passed all 300 tests across 41 suites after a 313.968-second build and 70.670-second test command. This includes the declared Orbital and CP share-input SY routes and collector rotation through internal SY balances in all four bindings and both policies. A subsequent 108-test selection across nine suites passed after a 517.541-second build and 77.720-second test command, covering both standard swap-fallback directions with funded epoch catchup in all four reserves and both liquidity policies, plus ten release packaging/Robinhood launch-stage checks. The latest combined regression passed 340 tests across 44 suites after a 37.678-second build and 79.889-second test command, including the real V4-backed Orbital binding, exact withdrawal optimization, relocated SY selectors and facet/delegate size checks. These are overlapping focused selections, not comparable full-suite timing claims. The independent full baseline build took 37,045.157 seconds and its hermetic retry took 9,718.795 seconds (30,056 pass / 3,632 fail with documented network-source exclusions). A full baseline is not green; comparison and failure attribution remain necessary. The estimate can grow if in-scope integration exposes additional defects. The owner explicitly resolved the automatic-review cleanup decision; the cleanup is regenerated and applied, with selected Solidity and frontend validation passed. Full repository acceptance is still pending. It does not alter D60 or D66 scope.
