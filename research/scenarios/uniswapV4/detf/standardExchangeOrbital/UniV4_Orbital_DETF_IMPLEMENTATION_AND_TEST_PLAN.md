# Uni V4 Orbital DETF — Implementation Plan

## Status

Pending CP DETF D0–D1 patterns. Same D0–D9 ladder under  
`scripts/foundry/research/uniswapV4/detf/standardExchangeOrbital/`.

## Profile

`FOUNDRY_PROFILE=se_orbital_detf` (via_ir). Watch for `scripts/**` skip in profile — may need default profile or profile patch.

## Milestones

M0 fixture → M1 D0–D1 → M2 gates → M3 expansion/compound/claim-mature → FINDINGS.
