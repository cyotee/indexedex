# Track B infographics for X

Labeled 1600×900 boards. Product voice and funded-design claims. Do not generate these in an image model.

Export from the repo root, with DTF Playwright installed:

```bash
node marketing/content/infographics/export.mjs
```

Writes `out/*.png` and copies each file to `../track-b/<piece>/board.png`.
