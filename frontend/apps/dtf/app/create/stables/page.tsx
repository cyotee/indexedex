import { CreatePageFrame } from '../CreatePageFrame'

export const metadata = {
  title: 'Create: three dollar vaults — Down To Finance',
}

export default function CreateStablesPage() {
  return <CreatePageFrame initialTypeId="stables" />
}
