/** Workspace package marker — expanded in PR2 with addresses/ABIs/chains/helpers. */
export const PROTOCOL_PACKAGE = true as const
export const PROTOCOL_PACKAGE_NAME = '@indexedex/protocol' as const
